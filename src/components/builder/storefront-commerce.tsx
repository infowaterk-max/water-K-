import type {CSSProperties,ReactNode} from 'react';
import {StorefrontRendererRegistry,type StorefrontComponentRenderProps} from '@/components/builder/storefront-runtime-renderer';
import {createStorefrontPrimitiveRendererRegistry} from '@/components/builder/storefront-primitives';
import type {StorefrontResolvedComponentNode} from '@/lib/builder/storefront-runtime';

export const STOREFRONT_CORE_COMMERCE_RENDERERS_VERSION='shoporation.storefront-core-commerce-renderers.v1' as const;
const text=(value:unknown,fallback='')=>typeof value==='string'?value:fallback;
const number=(value:unknown,fallback=0)=>typeof value==='number'&&Number.isFinite(value)?value:fallback;
const bool=(value:unknown,fallback=false)=>typeof value==='boolean'?value:fallback;
const record=(value:unknown):Record<string,unknown>|null=>value&&typeof value==='object'&&!Array.isArray(value)?value as Record<string,unknown>:null;
const rows=(value:unknown):Record<string,unknown>[]=>Array.isArray(value)?value.flatMap(item=>{const row=record(item);return row?[row]:[]}):[];
const span=(node:StorefrontResolvedComponentNode):CSSProperties=>({gridColumn:`span ${node.resolved.gridSpan} / span ${node.resolved.gridSpan}`});
const safeHref=(value:unknown,fallback='#')=>{if(typeof value!=='string'||!value.trim())return fallback;const href=value.trim();return href.startsWith('/')||href.startsWith('#')||href.startsWith('https://')||href.startsWith('mailto:')||href.startsWith('tel:')?href:fallback;};
const safeImage=(value:unknown)=>typeof value==='string'&&(value.startsWith('/')||value.startsWith('https://'))?value:null;
const money=(value:unknown,currencyValue:unknown='HUF')=>{if(typeof value==='string'&&value.trim())return value.trim();if(typeof value!=='number'||!Number.isFinite(value))return '';const currency=typeof currencyValue==='string'&&/^[A-Z]{3}$/.test(currencyValue)?currencyValue:'HUF';return new Intl.NumberFormat('hu-HU',{style:'currency',currency,maximumFractionDigits:currency==='HUF'?0:2}).format(value);};
const responsiveColumns=(requested:number,viewport:'desktop'|'tablet'|'mobile',mobile=2,tablet=3)=>viewport==='mobile'?Math.min(mobile,requested):viewport==='tablet'?Math.min(tablet,requested):requested;

type CommerceProduct={id:string;name:string;href:string;image:string|null;imageAlt:string;price:unknown;compareAtPrice:unknown;badge:string;stockLabel:string;subtitle:string};
const products=(value:unknown):CommerceProduct[]=>rows(value).map((row,index)=>({id:text(row.id,`product-${index}`),name:text(row.name,'Termék'),href:safeHref(row.href,'#'),image:safeImage(row.image),imageAlt:text(row.imageAlt,text(row.name,'Termék')),price:row.price,compareAtPrice:row.compareAtPrice,badge:text(row.badge),stockLabel:text(row.stockLabel),subtitle:text(row.subtitle,text(row.description))}));

function ProductCards({items,config}:{items:CommerceProduct[];config:Record<string,unknown>}){const presentation=text(config.presentation,'standard');const beauty=presentation==='beauty-lab'||presentation==='recommendation-beauty';const showBadges=bool(config.showBadges,true),showCompare=bool(config.showCompareAt,true),showCta=bool(config.showCta,false);return <>{items.map(product=><article key={product.id} data-storefront-commerce-card={presentation} style={{display:'flex',flexDirection:'column',gap:beauty?'.55rem':'.75rem',minWidth:0}}><a href={product.href} style={{color:'inherit',textDecoration:'none'}}><div style={{position:'relative',aspectRatio:text(config.imageRatio,beauty?'4 / 5':'4 / 5'),background:'var(--shoporation-color-surface-muted,#ece8df)',overflow:'hidden'}}>{product.image?<img src={product.image} alt={product.imageAlt} loading="lazy" style={{display:'block',width:'100%',height:'100%',objectFit:'cover'}}/>:null}{showBadges&&product.badge?<span style={{position:'absolute',top:'.5rem',left:'.5rem',padding:'.24rem .45rem',background:beauty?'var(--shoporation-color-accent,#a6aa92)':'var(--shoporation-color-background,#fff)',fontSize:'.61rem',fontWeight:700,letterSpacing:'.055em',textTransform:'uppercase'}}>{product.badge}</span>:null}</div></a><div style={{display:'grid',gap:'.2rem'}}><a href={product.href} style={{color:'inherit',textDecoration:'none',fontFamily:beauty?'var(--shoporation-heading-font,Georgia,serif)':undefined,fontWeight:beauty?500:650,fontSize:beauty?'1.02rem':undefined}}>{product.name}</a>{beauty&&product.subtitle?<small style={{color:'var(--shoporation-color-muted-text,#666)',lineHeight:1.35}}>{product.subtitle}</small>:null}<div style={{display:'flex',alignItems:'baseline',gap:'.45rem',flexWrap:'wrap'}}><strong>{money(product.price,config.currency)}</strong>{showCompare&&product.compareAtPrice?<small style={{color:'var(--shoporation-color-muted-text,#666)',textDecoration:'line-through'}}>{money(product.compareAtPrice,config.currency)}</small>:null}</div>{!beauty&&product.stockLabel?<small>{product.stockLabel}</small>:null}</div>{showCta?<a href={product.href} style={{marginTop:'auto',display:'inline-flex',justifyContent:'center',padding:'.62rem .75rem',background:'var(--shoporation-color-primary,#111)',color:'var(--shoporation-color-primary-contrast,#fff)',textDecoration:'none',fontSize:'.72rem',fontWeight:700,textTransform:beauty?'uppercase':undefined,letterSpacing:beauty?'.04em':undefined}}>{text(config.ctaLabel,'Kosárba')}</a>:null}</article>)}</>}

function CollectionNavigationRenderer({config,node,viewport}:StorefrontComponentRenderProps){const items=rows(config.items),requested=Math.max(2,Math.min(6,Math.round(number(config.columns,4)))),columns=responsiveColumns(requested,viewport,2,3),beauty=text(config.presentation)==='beauty-lab';return <section data-storefront-commerce="collection-navigation" data-presentation={text(config.presentation)||undefined} style={{...span(node),display:'grid',gap:'1.25rem'}}>{text(config.title)?<h2 style={{margin:0,fontFamily:'var(--shoporation-heading-font,Georgia,serif)',fontWeight:500,fontSize:beauty?'clamp(1.9rem,3vw,2.8rem)':undefined}}>{text(config.title)}</h2>:null}<div style={{display:'grid',gridTemplateColumns:`repeat(${columns},minmax(0,1fr))`,gap:beauty?'.6rem':'1rem'}}>{items.map((item,index)=>{const image=safeImage(item.image),label=text(item.label,`Kollekció ${index+1}`);return <a key={`${label}:${index}`} href={safeHref(item.href)} style={{color:'inherit',textDecoration:'none',display:'grid',gap:'.55rem'}}><div style={{aspectRatio:text(config.imageRatio,'1 / 1'),background:'var(--shoporation-color-surface-muted,#eee)',overflow:'hidden'}}>{image?<img src={image} alt={text(item.imageAlt,label)} style={{display:'block',width:'100%',height:'100%',objectFit:'cover'}}/>:null}</div><span style={{display:'flex',justifyContent:'space-between',gap:'.5rem',fontSize:beauty?'.78rem':undefined,textTransform:beauty?'uppercase':undefined,letterSpacing:beauty?'.045em':undefined}}><strong>{label}</strong><span aria-hidden="true">↗</span></span></a>})}</div></section>}
function CollectionHeaderRenderer({config,node}:StorefrontComponentRenderProps){const image=safeImage(config.image),align=text(config.align,'left')==='center'?'center':'left';return <header data-storefront-commerce="collection-header" data-presentation={text(config.presentation)||undefined} style={{...span(node),display:'grid',gap:'1rem',textAlign:align,paddingBlock:'clamp(2rem,6vw,6rem)'}}>{text(config.eyebrow)?<small style={{letterSpacing:'.14em',textTransform:'uppercase'}}>{text(config.eyebrow)}</small>:null}<h1 style={{margin:0,fontFamily:'var(--shoporation-heading-font,Georgia,serif)',fontSize:'clamp(2.4rem,6vw,5.5rem)',fontWeight:500,lineHeight:.95}}>{text(config.title,'Kollekció')}</h1>{text(config.description)?<p style={{margin:align==='center'?'0 auto':'0',maxWidth:'52rem',lineHeight:1.7,color:'var(--shoporation-color-muted-text,#666)'}}>{text(config.description)}</p>:null}{image?<img src={image} alt={text(config.imageAlt)} loading="eager" style={{display:'block',width:'100%',maxHeight:'42rem',objectFit:'cover'}}/>:null}</header>}
function ProductGridRenderer({config,node,viewport}:StorefrontComponentRenderProps){const items=products(config.products),requested=Math.max(2,Math.min(6,Math.round(number(config.columns,4)))),columns=responsiveColumns(requested,viewport,2,3),beauty=text(config.presentation)==='beauty-lab';return <section data-storefront-commerce="product-grid" data-presentation={text(config.presentation)||undefined} style={{...span(node),display:'grid',gap:'1.25rem'}}>{text(config.title)?<h2 style={{margin:0,fontFamily:'var(--shoporation-heading-font,Georgia,serif)',fontWeight:500,fontSize:beauty?'clamp(1.9rem,3vw,2.8rem)':undefined}}>{text(config.title)}</h2>:null}{items.length?<div style={{display:'grid',gridTemplateColumns:`repeat(${columns},minmax(0,1fr))`,gap:beauty?'clamp(.65rem,1.4vw,1rem)':'1.25rem'}}><ProductCards items={items} config={config}/></div>:<p>{text(config.emptyLabel,'Jelenleg nincs megjeleníthető termék.')}</p>}</section>}

function ProductGalleryRenderer({config,node,viewport}:StorefrontComponentRenderProps){const images=rows(config.images).map((item,index)=>({src:safeImage(item.src),alt:text(item.alt,`Termékkép ${index+1}`)})).filter(item=>item.src);const presentation=text(config.presentation);if(presentation==='editorial-thumbnails'){const main=images[0];const mobile=viewport==='mobile';return <div data-storefront-commerce="product-gallery" data-presentation={presentation} style={{...span(node),display:'grid',gridTemplateColumns:mobile?'1fr':'clamp(3.1rem,7vw,4.5rem) minmax(0,1fr)',gap:'.65rem',alignItems:'start'}}>{mobile?<div id="product-main" style={{aspectRatio:text(config.aspectRatio,'4 / 5'),background:'var(--shoporation-color-surface-muted,#eee9e2)',overflow:'hidden'}}>{main?<img src={main.src??''} alt={main.alt} loading="eager" style={{display:'block',width:'100%',height:'100%',objectFit:'cover'}}/>:null}</div>:null}<div style={{display:'grid',gridTemplateColumns:mobile?'repeat(4,minmax(0,1fr))':'1fr',gap:'.5rem'}}>{images.slice(0,4).map((item,index)=><a key={`${item.src}:${index}`} href={index===0?'#product-main':`#product-image-${index+1}`} aria-label={`${index+1}. termékkép`} style={{display:'block',aspectRatio:'1 / 1',border:index===0?'1px solid var(--shoporation-color-text,#111)':'1px solid var(--shoporation-color-border,#ddd)',overflow:'hidden'}}><img src={item.src??''} alt={item.alt} loading={index===0?'eager':'lazy'} style={{display:'block',width:'100%',height:'100%',objectFit:'cover'}}/></a>)}</div>{!mobile?<div id="product-main" style={{aspectRatio:text(config.aspectRatio,'4 / 5'),background:'var(--shoporation-color-surface-muted,#eee9e2)',overflow:'hidden'}}>{main?<img src={main.src??''} alt={main.alt} loading="eager" style={{display:'block',width:'100%',height:'100%',objectFit:'cover'}}/>:null}</div>:null}</div>}
return <div data-storefront-commerce="product-gallery" style={{...span(node),display:'grid',gridTemplateColumns:'repeat(2,minmax(0,1fr))',gap:'.75rem'}}>{images.length?images.map((item,index)=><img key={`${item.src}:${index}`} src={item.src??''} alt={item.alt} loading={index===0?'eager':'lazy'} style={{display:'block',width:'100%',height:'100%',minHeight:index===0?'28rem':'16rem',objectFit:'cover',gridColumn:index===0?'span 2':'span 1'}}/>):<div style={{gridColumn:'span 2',aspectRatio:text(config.aspectRatio,'4 / 5'),background:'var(--shoporation-color-surface-muted,#eee9e2)'}}/>}</div>}
function ProductInfoRenderer({config,node}:StorefrontComponentRenderProps){const badges=Array.isArray(config.badges)?config.badges.filter(item=>typeof item==='string') as string[]:[],beauty=text(config.presentation)==='beauty-lab';return <section data-storefront-commerce="product-info" data-presentation={text(config.presentation)||undefined} style={{...span(node),display:'grid',gap:'.7rem'}}>{badges.length?<div style={{display:'flex',gap:'.4rem',flexWrap:'wrap'}}>{badges.map(item=><span key={item} style={{background:beauty?'var(--shoporation-color-accent,#a6aa92)':'transparent',border:beauty?'0':'1px solid var(--shoporation-color-border,#ddd)',padding:'.22rem .48rem',fontSize:'.62rem',fontWeight:700,textTransform:'uppercase'}}>{item}</span>)}</div>:null}{text(config.eyebrow)?<small style={{letterSpacing:'.14em',textTransform:'uppercase'}}>{text(config.eyebrow)}</small>:null}<h1 style={{margin:0,fontFamily:'var(--shoporation-heading-font,Georgia,serif)',fontSize:beauty?'clamp(2.2rem,4vw,3.7rem)':'clamp(2.25rem,5vw,4.5rem)',fontWeight:500,lineHeight:.95,letterSpacing:'-.035em'}}>{text(config.title,'Termék')}</h1><div style={{display:'flex',gap:'.7rem',alignItems:'baseline',flexWrap:'wrap'}}>{money(config.price,config.currency)?<strong style={{fontSize:'1.08rem'}}>{money(config.price,config.currency)}</strong>:null}{money(config.compareAtPrice,config.currency)?<span style={{color:'var(--shoporation-color-muted-text,#666)',textDecoration:'line-through'}}>{money(config.compareAtPrice,config.currency)}</span>:null}</div>{text(config.description)?<p style={{margin:0,lineHeight:1.55,color:'var(--shoporation-color-muted-text,#555)'}}>{text(config.description)}</p>:null}{text(config.stockLabel)?<small style={{fontWeight:650}}>{text(config.stockLabel)}</small>:null}</section>}

type VariantOption={id:string;label:string;available:boolean;selected:boolean;href:string;swatch:string|null};
const options=(value:unknown):VariantOption[]=>rows(value).map((row,index)=>{const swatch=text(row.swatch);return{id:text(row.id,`option-${index}`),label:text(row.label,text(row.value,`Opció ${index+1}`)),available:row.available!==false,selected:row.selected===true,href:safeHref(row.href,''),swatch:/^#[0-9a-fA-F]{3,8}$/.test(swatch)?swatch:null}});
function VariantSwatchesRenderer({config,node}:StorefrontComponentRenderProps){const items=options(config.options),chips=text(config.presentation)==='chips';return <fieldset data-storefront-commerce="variant-swatches" data-presentation={text(config.presentation)||undefined} style={{...span(node),border:0,padding:0,margin:0,display:'grid',gap:'.55rem'}}><legend style={{fontWeight:650,fontSize:'.82rem'}}>{text(config.label,'Variáns')}</legend><div style={{display:'flex',gap:'.45rem',flexWrap:'wrap'}}>{items.map(item=>{const style:CSSProperties=chips?{display:'inline-flex',padding:'.52rem .72rem',border:item.selected?'1.5px solid var(--shoporation-color-text,#111)':'1px solid var(--shoporation-color-border,#bbb)',background:item.selected?'var(--shoporation-color-surface,#eee)':'transparent',color:'inherit',textDecoration:'none',opacity:item.available?1:.35,fontSize:'.75rem'}:{width:'2.15rem',height:'2.15rem',display:'inline-flex',borderRadius:'999px',border:item.selected?'2px solid var(--shoporation-color-text,#111)':'1px solid var(--shoporation-color-border,#bbb)',background:item.swatch??'transparent',opacity:item.available?1:.35};return item.available&&item.href?<a key={item.id} href={item.href} aria-current={item.selected?'true':undefined} style={style}>{chips?item.label:null}</a>:<span key={item.id} aria-disabled={!item.available||undefined} aria-current={item.selected?'true':undefined} style={style}>{chips?item.label:null}</span>})}</div></fieldset>}
function SizeSelectorRenderer({config,node}:StorefrontComponentRenderProps){const items=options(config.options);return <fieldset data-storefront-commerce="size-selector" data-presentation={text(config.presentation)||undefined} style={{...span(node),border:0,padding:0,margin:0,display:'grid',gap:'.55rem'}}><legend style={{fontWeight:650,fontSize:'.82rem'}}>{text(config.label,'Méret')}</legend><div style={{display:'flex',gap:'.45rem',flexWrap:'wrap'}}>{items.map(item=><span key={item.id} aria-disabled={!item.available||undefined} aria-current={item.selected?'true':undefined} style={{padding:'.52rem .72rem',border:item.selected?'1.5px solid var(--shoporation-color-text,#111)':'1px solid var(--shoporation-color-border,#bbb)',opacity:item.available?1:.35,textDecoration:item.available?'none':'line-through',fontSize:'.75rem'}}>{item.label}</span>)}</div></fieldset>}
function ReviewSummaryRenderer({config,node}:StorefrontComponentRenderProps){const rating=Math.max(0,Math.min(5,number(config.rating,0))),count=Math.max(0,Math.round(number(config.count,0))),beauty=text(config.presentation)==='beauty-lab';return <aside data-storefront-commerce="review-summary" data-presentation={text(config.presentation)||undefined} style={{...span(node),display:'flex',gap:'.45rem',alignItems:'center',flexWrap:'wrap',fontSize:beauty?'.78rem':undefined}}><span aria-label={`${rating.toFixed(1)} / 5`}>{'★'.repeat(Math.round(rating))}{'☆'.repeat(5-Math.round(rating))}</span><strong>{rating.toFixed(1)}</strong><span style={{color:'var(--shoporation-color-muted-text,#666)'}}>{text(config.label,count?`${count} értékelés`:'Még nincs értékelés')}</span></aside>}
function ContentTabsRenderer({config,node,viewport}:StorefrontComponentRenderProps){const tabs=rows(config.tabs);return <section data-storefront-commerce="content-tabs" data-presentation={text(config.presentation)||undefined} style={{...span(node),borderTop:'1px solid var(--shoporation-color-border,#ddd)',borderBottom:'1px solid var(--shoporation-color-border,#ddd)'}}><nav aria-label="Termékinformáció" style={{display:'flex',gap:viewport==='mobile'?'.85rem':'1.8rem',overflowX:'auto',padding:'1rem 0',borderBottom:'1px solid var(--shoporation-color-border,#ddd)'}}>{tabs.map((tab,index)=><a key={text(tab.id,`${index}`)} href={`#${text(tab.id,`product-tab-${index}`)}`} style={{color:'inherit',textDecoration:'none',fontSize:'.75rem',fontWeight:700,textTransform:'uppercase',letterSpacing:'.05em',whiteSpace:'nowrap'}}>{text(tab.label,text(tab.title,`Részlet ${index+1}`))}</a>)}</nav><div style={{display:'grid',gridTemplateColumns:viewport==='desktop'?`repeat(${Math.max(1,Math.min(3,tabs.length))},minmax(0,1fr))`:'1fr',gap:'clamp(1.2rem,3vw,2.5rem)',padding:'clamp(1.5rem,3vw,2.5rem) 0'}}>{tabs.map((tab,index)=><article id={text(tab.id,`product-tab-${index}`)} key={text(tab.id,`${index}`)} style={{display:'grid',alignContent:'start',gap:'.55rem'}}><h3 style={{margin:0,fontFamily:'var(--shoporation-heading-font,Georgia,serif)',fontWeight:500,fontSize:'1.35rem'}}>{text(tab.title,text(tab.label))}</h3><p style={{margin:0,lineHeight:1.65,color:'var(--shoporation-color-muted-text,#555)',whiteSpace:'pre-line'}}>{text(tab.copy)}</p></article>)}</div></section>}
function RecommendationRowRenderer({config,node,viewport}:StorefrontComponentRenderProps){const items=products(config.products),requested=Math.max(2,Math.min(6,Math.round(number(config.columns,4)))),columns=responsiveColumns(requested,viewport,2,3),beauty=text(config.presentation)==='beauty-lab';return <section data-storefront-commerce="recommendation-row" data-presentation={text(config.presentation)||undefined} style={{...span(node),display:'grid',gap:'1.25rem'}}><h2 style={{margin:0,fontFamily:'var(--shoporation-heading-font,Georgia,serif)',fontWeight:500,fontSize:beauty?'clamp(1.8rem,3vw,2.6rem)':undefined}}>{text(config.title,'Ezek is tetszhetnek')}</h2>{items.length?<div style={{display:'grid',gridTemplateColumns:`repeat(${columns},minmax(0,1fr))`,gap:'.8rem'}}><ProductCards items={items} config={{...config,presentation:beauty?'recommendation-beauty':'recommendation'}}/></div>:<p>{text(config.emptyLabel,'Jelenleg nincs kapcsolódó ajánlat.')}</p>}</section>}

const cartLines=(value:unknown)=>rows(value).map((row,index)=>({id:text(row.id,`line-${index}`),name:text(row.name,'Termék'),quantity:Math.max(1,Math.round(number(row.quantity,1))),lineTotal:row.lineTotal??row.total,variantLabel:text(row.variantLabel)}));
function CartSummaryRenderer({config,node}:StorefrontComponentRenderProps){const lines=cartLines(config.lines);return <section data-storefront-commerce="cart-summary" style={{...span(node),display:'grid',gap:'1.5rem',maxWidth:'64rem',marginInline:'auto'}}><h1 style={{margin:0,fontFamily:'var(--shoporation-heading-font,serif)',fontWeight:500}}>Kosár</h1>{lines.length?<div style={{display:'grid',gap:'1rem'}}>{lines.map(line=><div key={line.id} style={{display:'grid',gridTemplateColumns:'1fr auto',gap:'1rem',paddingBlock:'1rem',borderBottom:'1px solid var(--shoporation-color-border,#ddd)'}}><div><strong>{line.name}</strong>{line.variantLabel?<small style={{display:'block'}}>{line.variantLabel}</small>:null}<small>{line.quantity} db</small></div><strong>{money(line.lineTotal,config.currency)}</strong></div>)}</div>:<p>{text(config.emptyLabel,'A kosarad jelenleg üres.')}</p>}{lines.length?<div style={{display:'grid',gap:'.7rem',justifySelf:'end',minWidth:'min(100%,20rem)'}}><div style={{display:'flex',justifyContent:'space-between',gap:'2rem'}}><span>Részösszeg</span><strong>{money(config.subtotal,config.currency)}</strong></div><div style={{display:'flex',justifyContent:'space-between',gap:'2rem'}}><span>Összesen</span><strong>{money(config.total,config.currency)}</strong></div><a href={safeHref(config.checkoutHref,'/penztar')} style={{display:'inline-flex',justifyContent:'center',padding:'.9rem 1.2rem',background:'var(--shoporation-color-primary,#171717)',color:'var(--shoporation-color-primary-contrast,#fff)',textDecoration:'none'}}>{text(config.checkoutLabel,'Tovább a pénztárhoz')}</a></div>:null}</section>}
function CheckoutSummaryRenderer({config,node}:StorefrontComponentRenderProps){const lines=cartLines(config.lines);return <aside data-storefront-commerce="checkout-summary" style={{...span(node),display:'grid',gap:'1rem',padding:'clamp(1.25rem,3vw,2rem)',background:'var(--shoporation-color-surface,#eee)',border:'1px solid var(--shoporation-color-border,#ddd)'}}><h2 style={{margin:0,fontFamily:'var(--shoporation-heading-font,serif)',fontWeight:500}}>Rendelés összegzése</h2>{lines.map(line=><div key={line.id} style={{display:'flex',justifyContent:'space-between',gap:'1rem'}}><span>{line.name} × {line.quantity}</span><strong>{money(line.lineTotal,config.currency)}</strong></div>)}<hr style={{width:'100%',border:0,borderTop:'1px solid var(--shoporation-color-border,#ddd)'}}/><div style={{display:'flex',justifyContent:'space-between'}}><span>Részösszeg</span><strong>{money(config.subtotal,config.currency)}</strong></div><div style={{display:'flex',justifyContent:'space-between'}}><span>Szállítás</span><strong>{money(config.shipping,config.currency)||text(config.shipping,'Kalkuláció szerint')}</strong></div><div style={{display:'flex',justifyContent:'space-between'}}><span>Összesen</span><strong>{money(config.total,config.currency)}</strong></div><small>{text(config.secureLabel,'Biztonságos rendelés')}</small></aside>}

const RENDERERS:readonly [string,number,(props:StorefrontComponentRenderProps)=>ReactNode][]=[['commerce.collection-navigation',1,CollectionNavigationRenderer],['commerce.collection-header',1,CollectionHeaderRenderer],['commerce.product-grid',1,ProductGridRenderer],['commerce.product-gallery',1,ProductGalleryRenderer],['commerce.product-info',1,ProductInfoRenderer],['commerce.variant-swatches',1,VariantSwatchesRenderer],['commerce.size-selector',1,SizeSelectorRenderer],['commerce.review-summary',1,ReviewSummaryRenderer],['commerce.content-tabs',1,ContentTabsRenderer],['commerce.recommendation-row',1,RecommendationRowRenderer],['commerce.cart-summary',1,CartSummaryRenderer],['commerce.checkout-summary',1,CheckoutSummaryRenderer]] as const;
export function createStorefrontCoreCommerceRendererRegistry(){const registry:StorefrontRendererRegistry=createStorefrontPrimitiveRendererRegistry();for(const[componentKey,componentVersion,renderer]of RENDERERS)registry.register(componentKey,componentVersion,renderer);return registry;}
