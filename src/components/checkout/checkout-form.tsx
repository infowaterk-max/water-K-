'use client';
import { useState } from 'react';
import { useCart } from '@/components/cart/cart-provider';
export function CheckoutForm(){const {cart,clear}=useCart();const [state,setState]=useState('idle');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState('sending');const fd=new FormData(e.currentTarget);const checkout=Object.fromEntries(fd.entries());const r=await fetch('/api/orders',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({checkout,items:cart.items})});if(r.ok){setState('done');clear();}else setState('error');}return <form className="checkout-form" onSubmit={submit}><h1>Pénztár</h1><div className="form-grid"><input name="name" required placeholder="Név / kapcsolattartó"/><input name="email" required type="email" placeholder="E-mail"/><input name="phone" required placeholder="Telefonszám"/><select name="customerType"><option value="retail">Lakossági</option><option value="company">Céges</option><option value="reseller">Viszonteladó</option></select><input name="billingAddress" required placeholder="Számlázási cím"/><input name="shippingAddress" required placeholder="Szállítási cím"/><select name="shippingMethod"><option value="foxpost">Foxpost</option><option value="gls">GLS</option><option value="mpl">MPL</option><option value="pickup">Személyes átvétel</option></select><select name="paymentMethod"><option value="kh_card">Bankkártya – K&H</option><option value="bank_transfer">Banki átutalás</option></select></div><button disabled={state==='sending'||cart.items.length===0}>{state==='sending'?'Feldolgozás…':'Tovább a rendeléshez'}</button>{state==='done'&&<p>A rendelési adatok rögzítése sikeres.</p>}{state==='error'&&<p>Nem sikerült a rendelés. Ellenőrizd az adatokat.</p>}</form>}
