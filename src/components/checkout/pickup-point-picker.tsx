'use client';
import{useEffect,useMemo,useState}from'react';
type Point={id:string;name:string;address:string;lat?:number;lng?:number};
type Response={points?:Point[];error?:string};
export function PickupPointPicker({provider,label}:{provider:string;label:string}){const[query,setQuery]=useState(''),[points,setPoints]=useState<Point[]>([]),[selected,setSelected]=useState<Point|null>(null),[loading,setLoading]=useState(false),[error,setError]=useState(''),[open,setOpen]=useState(false);useEffect(()=>{setSelected(null);setQuery('');setPoints([]);setError('');setOpen(false)},[provider]);useEffect(()=>{if(!open)return;const close=(e:KeyboardEvent)=>{if(e.key==='Escape')setOpen(false)};document.addEventListener('keydown',close);const previous=document.body.style.overflow;document.body.style.overflow='hidden';return()=>{document.removeEventListener('keydown',close);document.body.style.overflow=previous}},[open]);const mapUrl=useMemo(()=>selected?.lat!=null&&selected.lng!=null?`https://www.openstreetmap.org/export/embed.html?bbox=${selected.lng-.01}%2C${selected.lat-.006}%2C${selected.lng+.01}%2C${selected.lat+.006}&layer=mapnik&marker=${selected.lat}%2C${selected.lng}`:'',[selected]);async function search(){setLoading(true);setError('');try{const response=await fetch(`/api/shipping/${encodeURIComponent(provider)}/pickup-points?q=${encodeURIComponent(query)}`),payload=await response.json() as Response;if(!response.ok){setPoints([]);setError(payload.error??'A csomagpontok nem tölthetők be.');return}setPoints(payload.points??[]);if(!(payload.points??[]).length)setError('Nincs találat. Próbálj másik települést, irányítószámot vagy utcát.')}catch{setError('A csomagpontok betöltése nem sikerült.')}finally{setLoading(false)}}function choose(point:Point){setSelected(point);setOpen(false)}return <div className="pickupPicker"><input type="hidden" name="parcelPointId" value={selected?.id??''}/><div className="pickupPickerHead"><div><strong>{label} átvételi pont</strong><p className="muted">Válassz csomagpontot vagy automatát a kereshető listából.</p></div>{selected&&<span className="badge">Kiválasztva</span>}</div>{selected?<div className="pickupSelected"><div><strong>{selected.name}</strong><span>{selected.address}</span></div><div className="pickupSelectedActions"><button type="button" className="btn btnGhost" onClick={()=>setOpen(true)}>Másik pont</button>{selected.lat!=null&&selected.lng!=null&&<a className="btn btnGhost" target="_blank" rel="noreferrer" href={`https://www.openstreetmap.org/?mlat=${selected.lat}&mlon=${selected.lng}#map=17/${selected.lat}/${selected.lng}`}>Térkép</a>}</div>{mapUrl&&<iframe className="pickupMiniMap" title={`${selected.name} térképe`} src={mapUrl} loading="lazy"/>}</div>:<button type="button" className="btn btnGhost pickupOpen" onClick={()=>setOpen(true)}>Átvételi pont kiválasztása</button>}{!selected&&<input className="pickupRequired" required value="" onChange={()=>{}} tabIndex={-1} aria-label="Átvételi pont kiválasztása szükséges"/>}{open&&<div className="pickupModalBackdrop" role="presentation" onMouseDown={e=>{if(e.currentTarget===e.target)setOpen(false)}}><section className="pickupModal" role="dialog" aria-modal="true" aria-label={`${label} átvételi pont kiválasztása`}><div className="pickupModalHead"><div><span className="eyebrow">Átvételi pont</span><h2>{label}</h2><p className="muted">Keress településre, irányítószámra vagy utcára.</p></div><button type="button" className="pickupClose" aria-label="Bezárás" onClick={()=>setOpen(false)}>×</button></div><div className="pickupSearch"><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Pl. Budapest, 1117, Nagykáta" onKeyDown={e=>{if(e.key==='Enter'){e.preventDefault();void search()}}}/><button type="button" className="btn btnPrimary" onClick={()=>void search()} disabled={loading}>{loading?'Keresés…':'Keresés'}</button></div>{error&&<p className="errorNotice">{error}</p>}<div className="pickupResults" role="listbox" aria-label="Átvételi pontok">{points.map(point=><button type="button" key={point.id} className={selected?.id===point.id?'pickupPoint active':'pickupPoint'} onClick={()=>choose(point)}><span><strong>{point.name}</strong><small>{point.address}</small></span><b>{selected?.id===point.id?'Kiválasztva':'Ezt választom'}</b></button>)}</div></section></div>}</div>}
