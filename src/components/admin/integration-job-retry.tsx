'use client';
import{useState}from'react';import{useRouter}from'next/navigation';
export function IntegrationJobRetry({jobId,kind}:{jobId:string;kind:string}){const router=useRouter(),[busy,setBusy]=useState(false),[message,setMessage]=useState('');async function retry(){if(!window.confirm('Biztosan újraindítod ezt az integrációs feladatot? A rendszer ellenőrzi, hogy nincs-e már aktív példány.'))return;setBusy(true);setMessage('');const r=await fetch(`/api/admin/integration-jobs/${jobId}/retry`,{method:'POST'}),b=await r.json().catch(()=>({}));setBusy(false);if(!r.ok){setMessage(b.error??'Az újraindítás nem sikerült.');return}setMessage(b.alreadyActive?'Már van aktív feladat, nem készült másolat.':'Újrapróbálás sorba állítva.');router.refresh()}return <div style={{display:'grid',gap:6,justifyItems:'start'}}><button type="button" className="btn btnGhost" disabled={busy} onClick={retry}>{busy?'Indítás…':'Újrapróbálás'}</button>{message&&<small className="muted" role="status">{message}</small>}<span className="srOnly">Feladattípus: {kind}</span></div>}
