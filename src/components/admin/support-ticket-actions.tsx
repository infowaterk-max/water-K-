'use client';import{useState}from'react';import{useRouter}from'next/navigation';
export function SupportTicketActions({id,status,priority,adminNote}:{id:string;status:string;priority:string;adminNote:string|null}){const router=useRouter(),[busy,setBusy]=useState(false),[note,setNote]=useState(adminNote??''),[p,setP]=useState(priority),[message,setMessage]=useState('');async function save(nextStatus=status){setBusy(true);setMessage('');const r=await fetch(`/api/admin/support/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({status:nextStatus,priority:p,adminNote:note||null})}),b=await r.json().catch(()=>({}));setBusy(false);if(!r.ok){setMessage(b.error??'Mentési hiba.');return}setMessage('Mentve.');router.refresh()}return <div style={{display:'grid',gap:8,minWidth:320}}><select aria-label="Prioritás" value={p} onChange={e=>setP(e.target.value)}><option value="low">Alacsony</option><option value="normal">Normál</option><option value="high">Magas</option><option value="urgent">Sürgős</option></select><textarea aria-label="Belső megjegyzés" rows={3} value={note} onChange={e=>setNote(e.target.value)} placeholder="Belső megjegyzés"/><div className="actions"><button className="btn btnGhost" disabled={busy} onClick={()=>save('in_progress')}>Folyamatban</button><button className="btn btnGhost" disabled={busy} onClick={()=>save('waiting_customer')}>Ügyfélre vár</button><button className="btn btnPrimary" disabled={busy} onClick={()=>save('resolved')}>Megoldva</button><button className="btn btnGhost" disabled={busy} onClick={()=>save('closed')}>Lezárás</button></div>{message&&<small className="muted">{message}</small>}</div>}
