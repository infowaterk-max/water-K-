'use client';import{useState}from'react';import{useRouter}from'next/navigation';
export function ReturnCaseActions({id,status,refundAmount,refundReference,inventoryRestockedAt}:{id:string;status:string;refundAmount:number|null;refundReference:string|null;inventoryRestockedAt?:string|null}){const router=useRouter(),[busy,setBusy]=useState(false),[message,setMessage]=useState(''),[amount,setAmount]=useState(refundAmount===null?'':String(refundAmount)),[reference,setReference]=useState(refundReference??'');async function update(next:string,restock=false){setBusy(true);setMessage('');const r=await fetch(`/api/admin/returns/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({status:next,refundAmount:amount===''?null:Number(amount),refundReference:reference||null,restock})}),b=await r.json().catch(()=>({}));setBusy(false);if(!r.ok){setMessage(b.error??'A módosítás nem sikerült.');return}setMessage(restock?'Készlet visszaállítva.':'Mentve.');router.refresh()}const received=['received','refund_pending','refunded','closed'].includes(status);return <div style={{display:'grid',gap:8,minWidth:280}}><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:8}}><input aria-label="Visszatérítés összege" type="number" min="0" placeholder="Összeg Ft" value={amount} onChange={e=>setAmount(e.target.value)}/><input aria-label="Visszatérítés hivatkozása" placeholder="Banki hivatkozás" value={reference} onChange={e=>setReference(e.target.value)}/></div><div className="actions">{status==='requested'&&<><button className="btn btnGhost" disabled={busy} onClick={()=>update('approved')}>Jóváhagyás</button><button className="btn btnGhost" disabled={busy} onClick={()=>update('rejected')}>Elutasítás</button></>}{status==='approved'&&<button className="btn btnGhost" disabled={busy} onClick={()=>update('received')}>Visszaérkezett</button>}{status==='received'&&<button className="btn btnGhost" disabled={busy} onClick={()=>update('refund_pending')}>Visszatérítés indul</button>}{['received','refund_pending'].includes(status)&&<button className="btn btnPrimary" disabled={busy} onClick={()=>update('refunded')}>Visszatérítve</button>}{status!=='closed'&&status!=='requested'&&<button className="btn btnGhost" disabled={busy} onClick={()=>update('closed')}>Lezárás</button>}{received&&!inventoryRestockedAt&&<button className="btn btnPrimary" disabled={busy} onClick={()=>update(status,true)}>Újraértékesíthető · készletre</button>}</div>{inventoryRestockedAt&&<small className="muted">A termék készlete már vissza lett állítva.</small>}{message&&<small className="muted">{message}</small>}</div>}
