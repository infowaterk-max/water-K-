'use client';import{useState}from'react';import{useRouter}from'next/navigation';export function OperationsCycleButton(){const[busy,setBusy]=useState(false);const router=useRouter();return <button className="btn" disabled={busy} onClick={async()=>{setBusy(true);try{const r=await fetch('/api/admin/operations/run',{method:'POST'});if(!r.ok)throw new Error();router.refresh()}finally{setBusy(false)}}}>{busy?'Feldolgozás…':'Műveleti ciklus futtatása'}</button>}export function OrderOperationAction({orderId,status}:{orderId:string;status:string}){const[busy,setBusy]=useState(false);const router=useRouter();const next=status==='reserved'?'ready_to_pack':status==='ready_to_pack'?'packed':status==='packed'?'handed_over':status==='handed_over'?'delivered':null;if(!next)return null;const label=next==='ready_to_pack'?'Csomagolásra kész':next==='packed'?'Csomagolva':next==='handed_over'?'Átadva futárnak':'Kézbesítve';return <button className="btn btnGhost" disabled={busy} onClick={async()=>{setBusy(true);try{const r=await fetch('/api/admin/operations/transition',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({orderId,targetStatus:next})});if(!r.ok){const j=await r.json().catch(()=>({}));alert(j.error??'Az állapotváltás nem sikerült.');return}router.refresh()}finally{setBusy(false)}}}>{busy?'Mentés…':label}</button>}
