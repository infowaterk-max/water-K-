'use client';import{useState}from'react';import{useRouter}from'next/navigation';import type{Product}from'@/lib/catalog';
type Operation='set_stock'|'adjust_stock'|'set_gross'|'set_net'|'activate'|'deactivate';export function BulkProductEditor({products}:{products:Product[]}){const router=useRouter(),[selected,setSelected]=useState<string[]>([]),[operation,setOperation]=useState<Operation>('adjust_stock'),[value,setValue]=useState(0),[busy,setBusy]=useState(false),[message,setMessage]=useState('');const noValue=operation==='activate'||operation==='deactivate';async function apply(){if(!selected.length)return;setBusy(true);setMessage('');const r=await fetch('/api/admin/catalog/bulk',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({ids:selected,operation,...(!noValue?{value}: {})})});const p=await r.json()as{error?:string;count?:number};setBusy(false);if(!r.ok){setMessage(p.error??'A művelet nem sikerült.');return}setMessage(`${p.count??selected.length} termék módosítva.`);setSelected([]);router.refresh()}return <section className="featurePanel"><span className="eyebrow">Atomi tömeges szerkesztés</span><h2>Jelöld ki a termékeket és válassz műveletet.</h2><div className="form-grid"><label>Művelet<select value={operation} onChange={e=>setOperation(e.target.value as Operation)}><option value="adjust_stock">Készlet növelése/csökkentése</option><option value="set_stock">Készlet beállítása</option><option value="set_gross">Bruttó ár beállítása</option><option value="set_net">Nettó ár beállítása</option><option value="activate">Aktiválás</option><option value="deactivate">Inaktiválás</option></select></label>{!noValue&&<label>Érték<input type="number" value={value} onChange={e=>setValue(Number(e.target.value))}/></label>}</div><div className="adminToolbar"><button className="btn btnGhost" type="button" onClick={()=>setSelected(selected.length===products.length?[]:products.map(p=>p.id))}>{selected.length===products.length?'Kijelölés törlése':'Összes kijelölése'}</button><button className="btn btnPrimary" type="button" disabled={busy||!selected.length} onClick={apply}>{busy?'Mentés…':`Alkalmazás ${selected.length} termékre`}</button></div>{message&&<p className="helperText">{message}</p>}<div className="integrationList">{products.map(p=><label key={p.id} className="card"><span><input type="checkbox" checked={selected.includes(p.id)} onChange={e=>setSelected(current=>e.target.checked?[...current,p.id]:current.filter(id=>id!==p.id))}/> <strong>{p.name}</strong></span><span className="muted">{p.stock} db · nettó {p.netPrice} Ft · bruttó {p.grossPrice} Ft</span></label>)}</div><p className="muted">A művelet egyetlen adatbázis-tranzakcióban fut. Ha bármelyik tétel hibás, egyik változás sem kerül mentésre.</p></section>}
