'use client';import{useState}from'react';
export function SupportForm(){const[busy,setBusy]=useState(false),[message,setMessage]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage('');const f=new FormData(e.currentTarget),payload={name:String(f.get('name')||''),email:String(f.get('email')||''),orderNumber:String(f.get('orderNumber')||''),category:String(f.get('category')||''),subject:String(f.get('subject')||''),message:String(f.get('message')||''),website:String(f.get('website')||'')};const r=await fetch('/api/support',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)}),b=await r.json().catch(()=>({}));setBusy(false);if(!r.ok){setMessage(b.error??'Az üzenet nem küldhető el.');return}setMessage(`Köszönjük! Az ügy száma: ${b.ticketNumber}`);e.currentTarget.reset()}return <form className="featurePanel" onSubmit={submit}><span className="eyebrow">Írj nekünk</span><h2>Ügyfélszolgálati üzenet</h2><div className="cards"><label>Név<input name="name" maxLength={120}/></label><label>E-mail<input name="email" type="email" required maxLength={200}/></label><label>Rendelésszám<input name="orderNumber" maxLength={80} placeholder="Ha van"/></label><label>Téma<select name="category" defaultValue="other"><option value="product">Termékkérdés</option><option value="order">Rendelés</option><option value="shipping">Szállítás</option><option value="invoice">Számla</option><option value="reseller">Viszonteladói ügy</option><option value="return">Visszaküldés / visszatérítés</option><option value="other">Egyéb</option></select></label></div><label>Tárgy<input name="subject" required maxLength={180}/></label><label>Üzenet<textarea name="message" required rows={6} minLength={10} maxLength={4000}/></label><input name="website" tabIndex={-1} autoComplete="off" style={{position:'absolute',left:'-9999px'}} aria-hidden="true"/><button className="btn btnPrimary" disabled={busy}>{busy?'Küldés…':'Üzenet elküldése'}</button>{message&&<p className="muted" role="status">{message}</p>}</form>}
