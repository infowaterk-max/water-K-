'use client';import{useState}from'react';export function NewsletterSignup(){const[email,setEmail]=useState(''),[consent,setConsent]=useState(false),[busy,setBusy]=useState(false),[message,setMessage]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMessage('');try{const r=await fetch('/api/marketing/newsletter',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,consent,source:'storefront_newsletter'})});const p=await r.json()as{error?:string;message?:string};setMessage(r.ok?p.message??'Sikeres feliratkozás.':p.error??'A feliratkozás nem sikerült.');if(r.ok){setEmail('');setConsent(false)}}catch{setMessage('A feliratkozás most nem sikerült.')}finally{setBusy(false)}}return <section className="featurePanel newsletterPanel"><span className="eyebrow">Hírlevél</span><h2>Hasznos tippek és ajánlatok e-mailben.</h2><p className="muted">A feliratkozás önkéntes, és nem feltétele a vásárlásnak. A rendelési e-mailek ettől teljesen függetlenek.</p><form onSubmit={submit}><div className="form-grid"><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="E-mail címed" autoComplete="email"/><button className="btn btnPrimary" disabled={busy||!consent}>{busy?'Feliratkozás…':'Feliratkozom'}</button></div><label className="inlineCheck"><input type="checkbox" checked={consent} onChange={e=>setConsent(e.target.checked)}/> Hozzájárulok marketing e-mailek küldéséhez. A hozzájárulás bármikor visszavonható.</label>{message&&<p className="helperText" role="status">{message}</p>}</form></section>}
