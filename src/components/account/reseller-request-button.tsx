'use client';
import{useState}from'react';import{useRouter}from'next/navigation';
export function ResellerRequestButton(){const[busy,setBusy]=useState(false),[message,setMessage]=useState(''),router=useRouter();async function request(){setBusy(true);setMessage('');try{const r=await fetch('/api/account/reseller-request',{method:'POST'}),b=await r.json().catch(()=>({}));setMessage(r.ok?(b.approved?'A partnerstátusz már aktív.':'Partnerigény elküldve.'):(b.error??'A partnerigény nem küldhető el.'));if(r.ok)router.refresh()}catch{setMessage('Hálózati hiba.')}finally{setBusy(false)}}return <div><button type="button" className="btn btnGhost" disabled={busy} onClick={request}>{busy?'Küldés…':'Viszonteladói partnerigény'}</button>{message&&<p className="muted" role="status">{message}</p>}</div>}
