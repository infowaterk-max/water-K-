export const DEFAULT_PAGE_SIZE=25;export const MAX_PAGE_SIZE=100;export function pagination(inputPage?:string|number|null,inputSize?:string|number|null){const p=Math.max(1,Number.parseInt(String(inputPage??1),10)||1);const s=Math.min(MAX_PAGE_SIZE,Math.max(1,Number.parseInt(String(inputSize??DEFAULT_PAGE_SIZE),10)||DEFAULT_PAGE_SIZE));const from=(p-1)*s;return{page:p,pageSize:s,from,to:from+s-1}}