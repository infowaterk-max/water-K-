import type{Metadata}from'next';import{getPublicContentBySlug}from'@/lib/content/server';import{RichContent}from'@/components/content/rich-content';export const metadata:Metadata={title:'Impresszum'};export default async function ImpressumPage(){const custom=await getPublicContentBySlug('page','impresszum');return <main className="section contentPage legalPageWrap"><div className="shell legalPage"><span className="eyebrow">Üzemeltetői adatok</span><h1 className="sectionTitle">{custom?.title??'Impresszum'}</h1>{custom?<section className="card"><RichContent body={custom.body}/></section>:<p className="notice"><strong>Az impresszum még nincs közzétéve.</strong> Az üzemeltető a Tartalom és SEO modulban töltheti fel a saját cégadatait tartalmazó oldalt.</p>}</div></main>}
