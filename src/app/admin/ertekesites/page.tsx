import{requirePlanFeature}from'@/lib/plans/access';import{requireCurrentStoreContext}from'@/lib/instances/scope';import{createAdminClient}from'@/lib/supabase/admin';import{formatHuf}from'@/lib/catalog';import{CommercialRefresh,OpportunityActions,OfferCreate,TaskActions,OfferActions}from'@/components/admin/commercial-actions';export const dynamic='force-dynamic';type Summary={channel:string;open_count:number;pipeline_net_huf:number;weighted_pipeline_net_huf:number;overdue_pipeline_net_huf:number};
export default async function SalesAdmin(){await requirePlanFeature('crm');const scope=await requireCurrentStoreContext('sales.manage');const a=createAdminClient();const[{data:s},{data:o},{data:t},{data:f},{data:v}]=await Promise.all([a.from('commercial_pipeline_summary').select('*').eq('instance_id',scope.instanceId),a.from('commercial_opportunities').select('id,channel,kind,status,priority_score,expected_value_net_huf,probability_percent,due_at,reason,recommended_action').eq('instance_id',scope.instanceId).in('status',['open','in_progress']).order('priority_score',{ascending:false}).limit(100),a.from('sales_tasks').select('id,title,status,priority,due_at,description').eq('instance_id',scope.instanceId).in('status',['open','in_progress']).order('priority',{ascending:false}).limit(50),a.from('commercial_offers').select('id,status,quantity,discount_percent,minimum_margin_percent,total_net_huf').eq('instance_id',scope.instanceId).in('status',['draft','approved','sent']).order('created_at',{ascending:false}).limit(50),a.from('product_variants').select('id,sku,label').eq('instance_id',scope.instanceId).eq('active',true).order('sku')]);const sums=(s??[])as Summary[],opps=o??[],tasks=t??[],offers=f??[],variants=v??[],total=sums.reduce((x,r)=>x+Number(r.pipeline_net_huf||0),0),weighted=sums.reduce((x,r)=>x+Number(r.weighted_pipeline_net_huf||0),0),overdue=sums.reduce((x,r)=>x+Number(r.overdue_pipeline_net_huf||0),0);return <section className="adminMain"><span className="eyebrow">Pro · Kereskedelmi irányítóközpont</span><h1 className="sectionTitle">Értékesítési pipeline</h1><p className="lead">A CRM lehetőségei, feladatai és ajánlatai kizárólag az aktuális webshophoz tartoznak.</p><CommercialRefresh/><div className="cards adminMetricCards"><div className="card"><span className="badge">Nyitott pipeline</span><div className="price">{formatHuf(total)}</div></div><div className="card"><span className="badge">Súlyozott forecast</span><div className="price">{formatHuf(weighted)}</div></div><div className="card"><span className="badge">Lejárt érték</span><div className="price">{formatHuf(overdue)}</div></div><div className="card"><span className="badge">Nyitott feladat</span><div className="price">{tasks.length}</div></div></div><section className="card"><h2>Legfontosabb lehetőségek</h2><div className="adminTableScroll"><table className="adminTable"><thead><tr><th>Prioritás</th><th>Csatorna</th><th>Típus</th><th>Várható érték</th><th>Esély</th><th>Javaslat</th><th>Művelet</th></tr></thead><tbody>{opps.map(x=><tr key={x.id}><td><strong>{x.priority_score}</strong></td><td>{x.channel.toUpperCase()}</td><td>{x.kind}</td><td>{formatHuf(Number(x.expected_value_net_huf))}</td><td>{Number(x.probability_percent).toFixed(0)}%</td><td>{x.recommended_action??x.reason}</td><td><OpportunityActions id={x.id}/><OfferCreate opportunityId={x.id} variants={variants}/></td></tr>)}</tbody></table></div></section><div className="cards"><section className="card"><h2>Értékesítési feladatok</h2>{tasks.map(x=><div key={x.id}><p><strong>{x.priority} · {x.title}</strong><br/><span className="muted">{x.description??'—'}</span></p><TaskActions id={x.id}/></div>)}</section><section className="card"><h2>Aktív ajánlatok</h2>{offers.map(x=><div key={x.id}><p><strong>{x.status} · {x.discount_percent}% kedvezmény</strong><br/><span className="muted">{x.quantity} db · minimum margin {x.minimum_margin_percent}% · {x.total_net_huf==null?'még nincs jóváhagyott snapshot':formatHuf(Number(x.total_net_huf))}</span></p><OfferActions id={x.id} status={x.status}/></div>)}</section></div></section>}
