import Link from'next/link';
import{requirePlanFeature}from'@/lib/plans/access';
import{getAdminContent}from'@/lib/content/server';
import{ContentManager}from'@/components/admin/content-manager';
import{requireCurrentStoreContext}from'@/lib/instances/scope';
import{getFeatureEntitlementDecisions}from'@/lib/entitlements/access';
export const dynamic='force-dynamic';
export default async function ContentAdmin(){await requirePlanFeature('contentMarketing');const scope=await requireCurrentStoreContext('marketing.manage');const[result,entitlements]=await Promise.all([getAdminContent({throwOnError:true}).then(data=>({data,error:false})).catch(()=>({data:[],error:true})),getFeatureEntitlementDecisions(scope.instanceId,['recipeCommerce','releaseCommerce'])]),items=result.data,recipeCommerce=entitlements.get('recipeCommerce')?.enabled===true,releaseCommerce=entitlements.get('releaseCommerce')?.enabled===true;return <section className="adminMain"><span className="eyebrow">Alap · Tartalom és keresőoptimalizálás</span><h1 className="sectionTitle">Tartalomkezelés</h1><p className="lead">Blogbejegyzések, kampányoldalak, jogi oldalak és keresőoptimalizálási mezők az aktuális webshophoz elkülönítve.</p><div className="actions"><Link className="btn btnPrimary" href="/admin/tartalom/builder">Visual Builder megnyitása</Link>{recipeCommerce?<Link className="btn" href="/admin/tartalom/receptek">Recipe Commerce receptkönyvtár</Link>:null}{releaseCommerce?<Link className="btn" href="/admin/tartalom/release-ek">Drop / Release könyvtár</Link>:null}</div>{result.error?<div className="errorNotice" role="alert"><strong>A tartalomlista most nem tölthető be.</strong> Hiányos állapotból tartalmat nem hozunk létre és nem módosítunk.</div>:<ContentManager items={items}/>}</section>;}
