import Link from'next/link';import{requirePlanFeature}from'@/lib/plans/access';import{formatHuf}from'@/lib/catalog';import{createAdminClient}from'@/lib/supabase/admin';import{CommunicationQueueButton}from'@/components/admin/communication-queue-button';export const dynamic='force-dynamic';type O={id:string;order_number:string;customer_id:string|null;customer_email:string;billing_name:string;status:string;total_gross_huf:number;created_at:string};type P={id:string;email:string|null;full_name:string|null;company_name:string|null;role:string;reseller_approved:boolean};type C={email:string;status:'granted'|'withdrawn';occurred_at:string};const paid=['paid','processing','shipped','completed'],days=(d:string)=>Math.max(0,Math.floor((Date.now()-+new Date(d))/86400000));export default async function Page(){await requirePlanFeature('crm');const a=createAdminClient();const[{data:od,error:oe},{data:pd,error:pe},{data:cd,error:ce},{data:sd}]=await Promise.all([a.from('orders').select('id,order_number,customer_id,customer_email,billing_name,status,total_gross_huf,created_at').order('created_at',{ascending:false}).limit(10000),a.from('profiles').select('id,email,full_name,company_name,role,reseller_approved').limit(5000),a.from('marketing_consents').select('email,status,occurred_at').eq('channel','email').order('occurred_at',{ascending:false}).limit(20000),a.from('communication_suppressions').select('email,active').eq('active',true).limit(20000)]);const orders=(od??[])as O[],profiles=(pd??[])as P[],consent=new Map<string,C>();for(const c of(cd??[])as C[]){const k=c.email.trim().toLowerCase();if(!consent.has(k))consent.set(k,c)}const suppressed=new Set((sd??[]).map(s=>s.email.trim().toLowerCase())),allowed=(e:string)=>consent.get(e.trim().toLowerCase())?.status==='granted'&&!suppressed.has(e.trim().toLowerCase()),now=Date.now(),payment=orders.filter(o=>o.status==='pending'&&now-+new Date(o.created_at)>=86400000&&now-+new Date(o.created_at)<7*86400000);const stats=new Map<string,{name:string;email:string;orders:number;revenue:number;last:string}>();for(const o of orders.filter(o=>paid.includes(o.status))){const k=o.customer_id??o.customer_email.toLowerCase(),s=stats.get(k)??{name:o.billing_name,email:o.customer_email,orders:0,revenue:0,last:o.created_at};s.orders++;s.revenue+=Number(o.total_gross_huf||0);if(+new Date(o.created_at)>+new Date(s.last))s.last=o.created_at;stats.set(k,s)}const all=[...stats.entries()].map(([key,s])=>({key,...s,days:days(s.last),allowed:allowed(s.email)})),risk=all.filter(c=>c.days>=30&&c.days<90),winback=all.filter(c=>c.days>=90&&c.days<180),vip=all.filter(c=>c.revenue>=100000&&c.days>=30),eligible=[...new Set([...risk,...winback,...vip].filter(c=>c.allowed).map(c=>c.email.toLowerCase()))].length,partners=profiles.filter(p=>p.role==='reseller'&&!p.reseller_approved),loadError=Boolean(oe||pe||ce);return <section className="adminMain"><span className="eyebrow">Pro · CRM utánkövetés</span><h1 className="sectionTitle">Kereskedelmi utánkövetési központ</h1><p className="lead">Életciklus-alapú ügyfélmegtartás, visszanyerés és egyedi utánkövetés.</p>{loadError&&<div className="errorNotice"><strong>Az adatok egy része nem tölthető be.</strong></div>}<div className="cards adminMetricCards"><div className="card"><span className="badge">Fizetésmentés</span><div className="price">{payment.length}</div></div><div className="card"><span className="badge">Marketingre jogosult</span><div className="price">{eligible}</div></div><div className="card"><span className="badge">Partner döntésre vár</span><div className="price">{partners.length}</div></div></div><section className="card"><h2>Fizetésre váró rendelések</h2>{payment.slice(0,20).map(o=><div key={o.id}><Link className="textLink" href={`/admin/rendelesek/${o.id}`}><strong>{o.order_number}</strong></Link> · {o.billing_name} · {formatHuf(o.total_gross_huf)} <CommunicationQueueButton kind="payment_followup" reference={o.id} label="Utánkövetés sorba"/></div>)}</section><section className="card"><h2>Nagy értékű kockázatos ügyfelek</h2>{vip.filter(c=>c.allowed).sort((a,b)=>b.revenue-a.revenue).slice(0,15).map(c=><div key={c.key}><strong>{c.name}</strong> · {c.days} nap · {formatHuf(c.revenue)} <CommunicationQueueButton kind={c.days>=90?'winback_90d':'repeat_30d'} reference={c.email} label="Egyedi utánkövetés"/></div>)}</section></section>}
