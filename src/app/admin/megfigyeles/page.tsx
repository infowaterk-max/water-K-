import Link from'next/link';import{createAdminClient}from'@/lib/supabase/admin';export const dynamic='force-dynamic';type K={events_24h:number;errors_24h:number;critical_24h:number;avg_duration_ms_24h:number;p95_duration_ms_24h:number;last_event_at:string|null};type R={id:number;correlation_id:string;category:string;severity:string;event_name:string;duration_ms:number|null;status_code:number|null;source:string;occurred_at:string};export default async function Page(){const a=createAdminClient();const[{data:k},{data:r}]=await Promise.all([a.from('observability_kpis').select('*').single(),a.from('observability_issue_queue').select('*').limit(100)]);const x=(k??{events_24h:0,errors_24h:0,critical_24h:0,avg_duration_ms_24h:0,p95_duration_ms_24h:0,last_event_at:null})as K,rows=(r??[])as R[];return <section className="adminMain"><span className="eyebrow">Platform · Rendszerfigyelés</span><h1 className="sectionTitle">Megfigyelési központ</h1><p className="lead">Adatvédelmi szempontból szűrt rendszeresemények, hibák és válaszidő-mutatók. Innen gyorsan látható, ha a platform vagy valamelyik kritikus integráció romló állapotot mutat.</p><div className="actions"><Link className="btn btnGhost" href="/api/health">Rendszerállapot</Link><Link className="btn btnGhost" href="/admin/integraciok">Integrációk</Link></div><div className="cards adminMetricCards"><div className="card"><span className="badge">Esemény 24h</span><div className="price">{x.events_24h}</div></div><div className="card"><span className="badge">Hiba 24h</span><div className="price">{x.errors_24h}</div></div><div className="card"><span className="badge">Kritikus</span><div className="price">{x.critical_24h}</div></div><div className="card"><span className="badge">Átlagos válaszidő</span><div className="price">{x.avg_duration_ms_24h} ms</div></div><div className="card"><span className="badge">P95 válaszidő</span><div className="price">{x.p95_duration_ms_24h} ms</div></div></div><section className="card"><h2>Figyelmet igénylő események</h2><div className="adminTableScroll"><table className="adminTable"><thead><tr><th>Idő</th><th>Súlyosság</th><th>Kategória</th><th>Esemény</th><th>Válaszidő</th><th>Korreláció</th></tr></thead><tbody>{rows.map(v=><tr key={v.id}><td>{new Date(v.occurred_at).toLocaleString('hu-HU')}</td><td>{v.severity}</td><td>{v.category}</td><td><strong>{v.event_name}</strong><div className="muted">{v.source} · HTTP {v.status_code??'–'}</div></td><td>{v.duration_ms??'–'} ms</td><td><code>{v.correlation_id.slice(0,18)}</code></td></tr>)}</tbody></table></div>{rows.length===0&&<p className="muted">Nincs figyelmeztető vagy kritikus esemény az elmúlt 7 napban.</p>}</section></section>}
