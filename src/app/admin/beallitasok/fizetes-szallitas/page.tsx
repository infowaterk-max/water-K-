import Link from 'next/link';
import { requireAdmin } from '@/lib/auth/require-admin';
import { getCommerceProviders,type CommerceProviderType } from '@/lib/commerce/providers';
import { configuredEnvironmentFields,getProviderGuide } from '@/lib/commerce/onboarding';
import { updateCommerceProviderAction,verifyCommerceProviderAction } from './actions';
export const dynamic='force-dynamic';
type Props={searchParams:Promise<{tab?:string}>};
const modeLabel={manual:'Kézi mód',api:'API integráció',builtin:'Beépített integráció',custom:'Egyedi API'} as const;
export default async function Page({searchParams}:Props){await requireAdmin();const q=await searchParams;const type:CommerceProviderType=q.tab==='szallitas'?'shipping':'payment';const providers=await getCommerceProviders(type);return <section className="adminMain"><span className="eyebrow">Shoperation · Beállítások</span><h1 className="sectionTitle">{type==='payment'?'Fizetési módok':'Szállítási módok'}</h1><p className="lead">A webshop tulajdonosa választ szolgáltatót és közvetlenül vele szerződik. A Shoperation a technikai kapcsolatot biztosítja, nem kényszerít egyetlen bankot vagy futárt sem.</p><div className="actions"><Link className={`btn ${type==='payment'?'btnPrimary':'btnGhost'}`} href="/admin/beallitasok/fizetes-szallitas?tab=fizetes">Fizetési módok</Link><Link className={`btn ${type==='shipping'?'btnPrimary':'btnGhost'}`} href="/admin/beallitasok/fizetes-szallitas?tab=szallitas">Szállítási módok</Link></div><div className="cards">{providers.map(p=>{const guide=getProviderGuide(p.code,p.connectionMode);const present=configuredEnvironmentFields(guide.requirements);const complete=guide.requirements.length===0||present.length===guide.requirements.length;return <article className="card" key={p.code}><div className="adminToolbar"><div><span className="badge">{modeLabel[p.connectionMode]}</span><h2>{p.name}</h2></div><span className="badge">{p.connectionStatus==='active'?'Használatra kész':p.enabled&&complete?'Hitelesítésre vár':p.enabled?'Beállítás szükséges':'Nincs bekapcsolva'}</span></div><p className="muted">{guide.contract}</p><div className="card"><strong>Bekötési ellenőrzőlista</strong>{guide.requirements.length===0?<p className="muted">Nincs szükség API-kulcsra.</p>:<ul>{guide.requirements.map(r=><li key={r.key}>{present.includes(r.key)?'✓':'○'} {r.label}{r.secret?' · titkos adat':''}</li>)}</ul>}<p className="muted">{guide.verification}</p>{guide.notes&&<p className="muted">{guide.notes}</p>}</div><form action={updateCommerceProviderAction} className="adminForm"><input type="hidden" name="providerCode" value={p.code}/><input type="hidden" name="enabled" value={p.enabled?'false':'true'}/><label>Vásárlóknak megjelenő név<input name="displayLabel" defaultValue={p.displayLabel??p.name}/></label>{type==='shipping'&&<label>Szállítási díj (Ft)<input name="feeHuf" type="number" min="0" defaultValue={p.feeHuf??0}/></label>}<button className={`btn ${p.enabled?'btnGhost':'btnPrimary'}`}>{p.enabled?'Kikapcsolás':'Szolgáltató kiválasztása'}</button></form>{p.enabled&&p.connectionMode!=='manual'&&<form action={verifyCommerceProviderAction}><input type="hidden" name="providerCode" value={p.code}/><button className="btn btnGhost">Kapcsolat ellenőrzése</button></form>}<p className="muted">A titkos API-kulcsok nem jelennek meg és nem kerülnek böngészőbe vagy adatbázis-konfigurációba; kizárólag szerveroldali titokként kezeljük őket.</p></article>})}</div></section>}
