import { createAdminClient } from '@/lib/supabase/admin';export const dynamic='force-dynamic';type Readiness={environment_key:string;display_name:string;source_sha:string|null;trusted_passes:number;trusted_failures:number;smoke_pass:boolean;security_pass:boolean;migration_pass:boolean;evidence_bundle_hash:string};type Decision={id:string;environment_key:string;source_sha:string;decision:string;note:string;created_at:string};export default async function RolloutPage(){const db=createAdminClient();const[{data:readiness},{data:decisions}]=await Promise.all([db.from('rollout_readiness').select('*').order('environment_key'),db.from('rollout_decisions').select('id,environment_key,source_sha,decision,note,created_at').order('created_at',{ascending:false}).limit(20)]);const rows=(readiness??[])as Readiness[],history=(decisions??[])as Decision[];return <section className="adminMain"><span className="eyebrow">Platform · Bevezetés</span><h1 className="sectionTitle">Rollout központ</h1><p className="lead">Preview, staging és production kapuk egy helyen. Itt látszik, melyik verzió áll készen a következő környezetre; a felület nem indít automatikus deployt.</p><div className="cards adminMetricCards">{rows.map(r=><article className="card" key={`${r.environment_key}:${r.source_sha??'none'}`}><span className="badge">{r.display_name}</span><div className="price">{r.trusted_failures>0?'NO-GO':r.smoke_pass&&(r.environment_key==='preview'||r.migration_pass)?'READY':'PENDING'}</div><p className="muted">{r.source_sha?.slice(0,12)??'nincs SHA'} · {r.trusted_passes} sikeres ellenőrzés</p></article>)}</div><section className="card"><h2>Környezeti kapuk</h2><div className="adminTableScroll"><table className="adminTable"><thead><tr><th>Környezet</th><th>SHA</th><th>Smoke</th><th>Migráció</th><th>Biztonság</th><th>Hibák</th></tr></thead><tbody>{rows.map(r=><tr key={`${r.environment_key}:table`}><td>{r.display_name}</td><td><code>{r.source_sha?.slice(0,12)??'—'}</code></td><td>{r.smoke_pass?'OK':'—'}</td><td>{r.migration_pass?'OK':'—'}</td><td>{r.security_pass?'OK':'—'}</td><td>{r.trusted_failures}</td></tr>)}</tbody></table></div></section><section className="card"><h2>GO / NO-GO döntési napló</h2>{history.length===0?<p className="muted">Még nincs rollout döntés.</p>:<div className="adminTableScroll"><table className="adminTable"><thead><tr><th>Környezet</th><th>Döntés</th><th>SHA</th><th>Indoklás</th><th>Idő</th></tr></thead><tbody>{history.map(d=><tr key={d.id}><td>{d.environment_key}</td><td><strong>{d.decision.toUpperCase()}</strong></td><td><code>{d.source_sha.slice(0,12)}</code></td><td>{d.note}</td><td>{new Date(d.created_at).toLocaleString('hu-HU')}</td></tr>)}</tbody></table></div>}</section></section>}
