import{createAdminClient}from'@/lib/supabase/admin';import{formatHuf}from'@/lib/catalog';import{CustomerRoleControl}from'@/components/admin/customer-role-control';import{requireCurrentStoreContext}from'@/lib/instances/scope';
type P={id:string;email:string|null;full_name:string|null;company_name:string|null;tax_number:string|null;created_at:string};
type R={user_id:string;role:string;reseller_approved:boolean;reseller_requested_at:string|null;approved_at:string|null;created_at:string};
type M={customer_key:string;customer_id:string|null;email_key:string;paid_orders:number;revenue_gross_huf:number;aov_gross_huf:number;first_order_at:string;last_order_at:string;days_since_last_order:number;segment:string;cogs_net_huf:number|null;cogs_to_revenue_pct:number|null};
const labels:Record<string,string>={first_time:'Első vásárló',vip:'VIP',repeat:'Visszatérő',active:'Aktív',at_risk:'Kockázatos',winback:'Visszanyerendő',dormant:'Inaktív'};const weight:Record<string,number>={vip:7,at_risk:6,winback:5,dormant:4,repeat:3,first_time:2,active:1};
export const dynamic='force-dynamic';
export default async function Page(){
 const scope=await requireCurrentStoreContext('sales.manage'),a=createAdminClient();
 const[{data:rd,error:re},{data:md,error:me}]=await Promise.all([
  a.from('customer_instance_roles').select('user_id,role,reseller_approved,reseller_requested_at,approved_at,created_at').eq('instance_id',scope.instanceId).order('created_at',{ascending:false}).limit(5000),
  a.from('customer_commercial_metrics').select('*').eq('instance_id',scope.instanceId).limit(5000),
 ]);
 const roles=(rd??[])as R[],metrics=(md??[])as M[],ids=[...new Set(roles.map(r=>r.user_id))];
 const profileResult=ids.length?await a.from('profiles').select('id,email,full_name,company_name,tax_number,created_at').in('id',ids).limit(5000):{data:[] as P[],error:null};
 const profiles=(profileResult.data??[])as P[],profileById=new Map(profiles.map(p=>[p.id,p])),roleById=new Map(roles.map(r=>[r.user_id,r])),byId=new Map(metrics.filter(x=>x.customer_id).map(x=>[x.customer_id as string,x])),byEmail=new Map(metrics.map(x=>[x.email_key,x])),loadError=Boolean(re||me||profileResult.error);
 const enriched=roles.map(r=>{const p=profileById.get(r.user_id),m=byId.get(r.user_id)??(p?.email?byEmail.get(p.email.toLowerCase()):undefined);return{id:r.user_id,email:p?.email??null,full_name:p?.full_name??null,company_name:p?.company_name??null,tax_number:p?.tax_number??null,created_at:p?.created_at??r.created_at,role:r.role,reseller_approved:r.reseller_approved,reseller_requested_at:r.reseller_requested_at,approved_at:r.approved_at,metric:m,priority:(m?weight[m.segment]??1:0)+(r.role==='reseller'&&!r.reseller_approved?8:0)}});
 const count=(segment:string)=>metrics.filter(m=>m.segment===segment).length,totalRevenue=metrics.reduce((s,m)=>s+Number(m.revenue_gross_huf||0),0),totalOrders=metrics.reduce((s,m)=>s+Number(m.paid_orders||0),0),avgAov=totalOrders?Math.round(totalRevenue/totalOrders):0,repeatCustomers=metrics.filter(m=>m.paid_orders>=2).length,repeatRate=metrics.length?Math.round(repeatCustomers/metrics.length*100):0,pending=enriched.filter(c=>c.role==='reseller'&&!c.reseller_approved).length,atRiskRevenue=metrics.filter(m=>['at_risk','winback','dormant'].includes(m.segment)).reduce((s,m)=>s+Number(m.revenue_gross_huf||0),0),queue=enriched.filter(c=>c.priority>=4||(c.role==='reseller'&&!c.reseller_approved)).sort((x,y)=>y.priority-x.priority||Number(y.metric?.revenue_gross_huf||0)-Number(x.metric?.revenue_gross_huf||0)).slice(0,25);
 return <section className="adminMain"><span className="eyebrow">Admin · B2C/B2B ügyfélintelligencia</span><h1 className="sectionTitle">Ügyfélérték és partnerjóváhagyás</h1><p className="lead">Az ügyfél- és partnerstátusz kizárólag az aktuális webshophoz tartozik; egy másik Shoporation webshop jogosultságait nem örökli.</p>{loadError&&<div className="errorNotice"><strong>Az ügyféladatok egy része most nem tölthető be.</strong></div>}<div className="cards adminMetricCards"><div className="card"><span className="badge">Ügyfélérték</span><div className="price">{formatHuf(totalRevenue)}</div></div><div className="card"><span className="badge">AOV</span><div className="price">{formatHuf(avgAov)}</div></div><div className="card"><span className="badge">Repeat rate</span><div className="price">{repeatRate}%</div></div><div className="card"><span className="badge">Partnerigény</span><div className="price">{pending}</div></div></div><div className="splitFeature"><section className="featurePanel"><span className="eyebrow">Életciklus</span><h2>V9 szegmensek</h2><div className="integrationList"><div><span>VIP</span><strong>{count('vip')}</strong></div><div><span>Visszatérő</span><strong>{count('repeat')}</strong></div><div><span>Kockázatos</span><strong>{count('at_risk')}</strong></div><div><span>Visszanyerendő</span><strong>{count('winback')}</strong></div><div><span>Inaktív</span><strong>{count('dormant')}</strong></div></div></section><section className="featurePanel darkPanel"><span className="eyebrow">B2B</span><h2>{pending} jóváhagyásra váró partner</h2><p>A jóváhagyás csak ebben a webshopban aktivál partnerárat és professional termék-hozzáférést.</p></section></div>{queue.length>0&&<section className="featurePanel"><span className="eyebrow">Döntési munkasor</span><h2>Kikkel érdemes most foglalkozni?</h2><div className="integrationList">{queue.map(c=><div key={c.id}><span><span className="badge">{c.role==='reseller'&&!c.reseller_approved?'Partnerjóváhagyás':labels[c.metric?.segment??'']??'Új kapcsolat'}</span> <strong>{c.full_name||c.email||'Névtelen ügyfél'}</strong><br/><span className="muted">{c.company_name||'magánvásárló'} · {c.metric?`${c.metric.paid_orders} rendelés · AOV ${formatHuf(c.metric.aov_gross_huf)} · ${c.metric.days_since_last_order} napja aktív`:'még nincs fizetett vásárlás'}</span></span><strong>{formatHuf(c.metric?.revenue_gross_huf||0)}</strong></div>)}</div></section>}<div className="tableCard adminTableScroll"><table className="adminTable"><thead><tr><th>Ügyfél</th><th>Szegmens</th><th>LTV</th><th>Rendelések</th><th>Webshop-szerepkör</th><th>Partnerkezelés</th><th>Utolsó vásárlás</th></tr></thead><tbody>{enriched.map(c=><tr key={c.id}><td><strong>{c.full_name||'Nincs név'}</strong><br/><span className="muted">{c.email||'—'}</span>{c.company_name&&<><br/><span className="muted">{c.company_name}</span></>}</td><td><span className="badge">{labels[c.metric?.segment??'']??'Még nem vásárolt'}</span></td><td><strong>{formatHuf(c.metric?.revenue_gross_huf||0)}</strong></td><td>{c.metric?.paid_orders??0}</td><td><span className="badge">{c.role==='reseller'?(c.reseller_approved?'Viszonteladó':'Jóváhagyásra vár'):'Vásárló'}</span></td><td><CustomerRoleControl id={c.id} role={c.role} approved={c.reseller_approved}/></td><td>{c.metric?.last_order_at?new Intl.DateTimeFormat('hu-HU',{dateStyle:'short'}).format(new Date(c.metric.last_order_at)):<span className="muted">—</span>}</td></tr>)}</tbody></table></div><p className="muted">Kockázati történeti érték: {formatHuf(atRiskRevenue)}.</p></section>;
}
