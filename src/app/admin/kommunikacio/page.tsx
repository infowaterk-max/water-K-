import { createAdminClient } from '@/lib/supabase/admin';
import { communicationTemplates } from '@/lib/communication/templates';

export const dynamic='force-dynamic';
type Job={id:string;recipient_email:string;purpose:string;template_key:string;status:string;attempts:number;scheduled_at:string;sent_at:string|null;last_error:string|null;provider_message_id:string|null;created_at:string};
type WorkerRun={id:string;source:string;status:string;recovered:number;claimed:number;sent:number;failed:number;blocked:number;error_message:string|null;started_at:string;finished_at:string|null};
const statusLabel:Record<string,string>={pending:'Várakozik',processing:'Feldolgozás',sent:'Elküldve',failed:'Hibás',blocked:'Blokkolva',cancelled:'Törölve'};
const runStatusLabel:Record<string,string>={running:'Fut',success:'Sikeres',failed:'Hibás'};
export default async function CommunicationAdmin(){
 const admin=createAdminClient();
 const [{data:jobData,error:jobError},{data:runData,error:runError}]=await Promise.all([
  admin.from('communication_jobs').select('id,recipient_email,purpose,template_key,status,attempts,scheduled_at,sent_at,last_error,provider_message_id,created_at').order('created_at',{ascending:false}).limit(500),
  admin.from('communication_worker_runs').select('id,source,status,recovered,claimed,sent,failed,blocked,error_message,started_at,finished_at').order('started_at',{ascending:false}).limit(30)
 ]);
 const jobs=(jobData??[]) as Job[];const runs=(runData??[]) as WorkerRun[];const count=(s:string)=>jobs.filter(j=>j.status===s).length;const marketing=jobs.filter(j=>j.purpose==='marketing').length;const transactional=jobs.filter(j=>j.purpose==='transactional').length;const lastRun=runs[0]??null;const staleRunning=runs.filter(r=>r.status==='running'&&Date.now()-new Date(r.started_at).getTime()>30*60*1000).length;
 return <section className="adminMain"><span className="eyebrow">Admin · Kommunikáció</span><h1 className="sectionTitle">Kommunikációs műveleti központ</h1><p className="lead">Küldési sor, sablonok, worker-futások, hibák és auditállapot egy helyen. A szolgáltatói e-mail adapter bekötéséig ez a réteg nem küld külső levelet.</p>{(jobError||runError)&&<div className="errorNotice"><strong>A kommunikációs felügyelet egy része most nem olvasható.</strong></div>}
 <div className="cards adminMetricCards"><div className="card"><span className="badge">Várakozik</span><div className="price">{count('pending')}</div><p className="muted">ütemezett küldési feladat</p></div><div className="card"><span className="badge">Hibás / blokkolt</span><div className="price">{count('failed')+count('blocked')}</div><p className="muted">beavatkozást igényel</p></div><div className="card"><span className="badge">Utolsó worker</span><div className="price">{lastRun?runStatusLabel[lastRun.status]??lastRun.status:'—'}</div><p className="muted">{lastRun?new Intl.DateTimeFormat('hu-HU',{dateStyle:'short',timeStyle:'short'}).format(new Date(lastRun.started_at)):'még nem futott'}</p></div><div className="card"><span className="badge">Beragadt futás</span><div className="price">{staleRunning}</div><p className="muted">30+ perce futó worker napló</p></div></div>
 <section className="card"><span className="eyebrow">Worker felügyelet</span><h2>Legutóbbi futások</h2><div className="adminTableScroll"><table className="adminTable"><thead><tr><th>Kezdés</th><th>Forrás</th><th>Állapot</th><th>Helyreállított</th><th>Felvett</th><th>Elküldött</th><th>Hiba / blokkolt</th></tr></thead><tbody>{runs.map(r=><tr key={r.id}><td>{new Intl.DateTimeFormat('hu-HU',{dateStyle:'short',timeStyle:'short'}).format(new Date(r.started_at))}</td><td>{r.source}</td><td><span className="badge">{runStatusLabel[r.status]??r.status}</span>{r.error_message?<><br/><span className="muted">{r.error_message}</span></>:null}</td><td>{r.recovered}</td><td>{r.claimed}</td><td>{r.sent}</td><td>{r.failed} / {r.blocked}</td></tr>)}</tbody></table></div>{runs.length===0&&<p className="muted">Még nincs worker-futási napló.</p>}</section>
 <section className="card"><span className="eyebrow">Sablonregiszter</span><h2>Engedélyezett kommunikációs típusok</h2><div className="integrationList">{communicationTemplates.map(t=><div key={t.key}><span><strong>{t.subject}</strong><br/><span className="muted">{t.description}</span></span><strong>{t.purpose==='marketing'?'Marketing':'Tranzakciós'}</strong></div>)}</div></section>
 <section className="tableCard"><div className="adminTableScroll"><table className="adminTable"><thead><tr><th>Címzett</th><th>Típus / sablon</th><th>Állapot</th><th>Ütemezés</th><th>Próbálkozás</th><th>Szolgáltató / hiba</th></tr></thead><tbody>{jobs.map(j=><tr key={j.id}><td><strong>{j.recipient_email}</strong></td><td>{j.purpose==='marketing'?'Marketing':'Tranzakciós'}<br/><span className="muted">{j.template_key}</span></td><td><span className="badge">{statusLabel[j.status]??j.status}</span>{j.sent_at&&<><br/><span className="muted">{new Intl.DateTimeFormat('hu-HU',{dateStyle:'short',timeStyle:'short'}).format(new Date(j.sent_at))}</span></>}</td><td>{new Intl.DateTimeFormat('hu-HU',{dateStyle:'short',timeStyle:'short'}).format(new Date(j.scheduled_at))}</td><td>{j.attempts}</td><td>{j.provider_message_id?<span className="muted">{j.provider_message_id}</span>:j.last_error?<span className="muted">{j.last_error}</span>:<span className="muted">—</span>}</td></tr>)}</tbody></table></div>{jobs.length===0&&<p className="muted">Még nincs kommunikációs feladat a sorban.</p>}</section>
 <p className="muted">A napi Vercel cron a meglévő integrációs workerrel együtt fut. Marketingfeladat csak aktív hozzájárulással kerülhet a sorba, és küldés előtt újra ellenőrződik.</p></section>;
}
