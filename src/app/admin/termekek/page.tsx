import{formatHuf}from'@/lib/catalog';import{getProducts}from'@/lib/catalog-server';import{InventoryEditor}from'@/components/admin/inventory-editor';import{createAdminClient}from'@/lib/supabase/admin';export const dynamic='force-dynamic';const selling=['paid','processing','shipped','completed'];type Level='critical'|'warning'|'ok';const rank:Record<Level,number>={critical:0,warning:1,ok:2};
export default async function AdminProducts(){const products=await getProducts(),admin=createAdminClient(),now=Date.now(),since60=new Date(now-60*86400000).toISOString();const[{data:variantRows,error:pe},{data:orders,error:oe},{data:items,error:ie}]=await Promise.all([admin.from('product_variants').select('id,reseller_gross_price_huf,reseller_net_price_huf,unit_cost_net_huf'),admin.from('orders').select('id,created_at').gte('created_at',since60).in('status',selling),admin.from('order_items').select('order_id,variant_id,quantity,line_total_gross_huf')]);const variantData=new Map((variantRows??[]).map(r=>[r.id,r])),orderDate=new Map((orders??[]).map(o=>[o.id,+new Date(o.created_at)])),sold30=new Map<string,number>(),soldPrev=new Map<string,number>(),revenue30=new Map<string,number>();for(const i of items??[]){if(!i.variant_id)continue;const d=orderDate.get(i.order_id);if(!d)continue;if(d>=now-30*86400000){sold30.set(i.variant_id,(sold30.get(i.variant_id)??0)+i.quantity);revenue30.set(i.variant_id,(revenue30.get(i.variant_id)??0)+Number(i.line_total_gross_huf||0))}else soldPrev.set(i.variant_id,(soldPrev.get(i.variant_id)??0)+i.quantity)}const forecasts=products.map(p=>{const row=variantData.get(p.id),cost=row?.unit_cost_net_huf==null?null:Number(row.unit_cost_net_huf),sold=sold30.get(p.id)??0,prev=soldPrev.get(p.id)??0,daily=sold/30,days=daily>0?Math.floor(p.stock/daily):null,target=Math.ceil(daily*30),orderQty=Math.max(0,target-p.stock),level:Level=p.stock<=0||days!==null&&days<=14?'critical':days!==null&&days<=30?'warning':'ok',trend=prev?Math.round((sold-prev)/prev*100):sold?100:0,margin=cost===null?null:p.netPrice-cost,marginPct=cost===null||p.netPrice<=0?null:Math.round((p.netPrice-cost)/p.netPrice*1000)/10;return{id:p.id,name:p.name,stock:p.stock,sold,prev,daily,days,target,orderQty,level,trend,revenue:revenue30.get(p.id)??0,cost,margin,marginPct,purchaseCost:cost===null?null:orderQty*cost}}).sort((a,b)=>rank[a.level]-rank[b.level]||b.revenue-a.revenue),alerts=forecasts.filter(f=>f.level!=='ok'),purchase=forecasts.filter(f=>f.orderQty>0&&f.level!=='ok'),knownPurchaseCost=purchase.reduce((s,f)=>s+(f.purchaseCost??0),0),unknownCostCount=purchase.filter(f=>f.cost===null).length,sales30=forecasts.reduce((s,f)=>s+f.revenue,0),units30=forecasts.reduce((s,f)=>s+f.sold,0),knownCogs30=forecasts.reduce((s,f)=>s+(f.cost===null?0:f.cost*f.sold),0),knownNetSales30=forecasts.reduce((s,f)=>s+(f.cost===null?0:f.sold*p.netPrice),0),portfolioMargin=knownNetSales30>0?Math.round((knownNetSales30-knownCogs30)/knownNetSales30*1000)/10:null,loadError=Boolean(pe||oe||ie);return <section className="adminMain"><span className="eyebrow">Admin · Termékek</span><h1 className="sectionTitle">Készlet, önköltség és fedezet</h1><p className="lead">Fogyási sebesség, készletfedezet, nettó egységköltség és valódi utánrendelési tőkeigény egyetlen operatív nézetben.</p>{loadError&&<div className="errorNotice"><strong>A készletelemzés egy része most nem tölthető be.</strong></div>}<div className="cards adminMetricCards"><div className="card"><span className="badge">30 napos fogyás</span><div className="price">{units30} db</div><p className="muted">{formatHuf(sales30)} fizetett bruttó forgalom</p></div><div className="card"><span className="badge">Készletriasztás</span><div className="price">{alerts.length}</div><p className="muted">30 napon belüli kockázat</p></div><div className="card"><span className="badge">Beszerzendő</span><div className="price">{purchase.reduce((s,f)=>s+f.orderQty,0)} db</div><p className="muted">30 napos célkészlethez</p></div><div className="card"><span className="badge">Beszerzési tőkeigény</span><div className="price">{formatHuf(knownPurchaseCost)}</div><p className="muted">nettó önköltségen{unknownCostCount?` · ${unknownCostCount} tételnél hiányzik az önköltség`:''}</p></div><div className="card"><span className="badge">Ismert fedezeti ráta</span><div className="price">{portfolioMargin===null?'—':`${portfolioMargin}%`}</div><p className="muted">csak rögzített önköltségű, 30 napban eladott tételek</p></div></div>{purchase.length>0&&<section className="featurePanel"><span className="eyebrow">Beszerzési munkasor</span><h2>Mit érdemes most utánrendelni?</h2><div className="integrationList">{purchase.map(f=><div key={f.id}><span><span className="badge">{f.level==='critical'?'Kritikus':'Figyelendő'}</span> <strong>{f.name}</strong><br/><span className="muted">{f.stock} db készlet · {f.sold} db/30 nap · trend {f.trend>=0?'+':''}{f.trend}%</span></span><strong>+{f.orderQty} db · {f.purchaseCost===null?'önköltség hiányzik':formatHuf(f.purchaseCost)}</strong></div>)}</div><p className="muted">A tőkeigény nettó beszerzési/előállítási egységköltséggel számol. Az utánrendelési mennyiség továbbra is 30 napos célkészletre épül.</p></section>}<div className="tableCard adminTableScroll"><table className="adminTable"><thead><tr><th>Kiszerelés</th><th>Lakossági ár</th><th>Készlet és árak</th><th>Partnerár</th><th>Fedezet</th><th>Fogyás / trend</th><th>Készletfedezet</th><th>Javasolt rendelés</th></tr></thead><tbody>{products.map(p=>{const row=variantData.get(p.id),f=forecasts.find(x=>x.id===p.id)!;return <tr key={p.id}><td><strong>{p.name}</strong><br/><span className="muted">{p.slug}</span></td><td>{formatHuf(p.grossPrice)}<br/><span className="muted">nettó {formatHuf(p.netPrice)}</span></td><td><InventoryEditor id={p.id} stock={p.stock} grossPrice={p.grossPrice} netPrice={p.netPrice} resellerGrossPrice={row?.reseller_gross_price_huf??null} resellerNetPrice={row?.reseller_net_price_huf??null} unitCostNet={row?.unit_cost_net_huf??null}/></td><td>{row?.reseller_gross_price_huf!=null?<><strong>{formatHuf(row.reseller_gross_price_huf)}</strong><br/><span className="muted">nettó {row.reseller_net_price_huf!=null?formatHuf(row.reseller_net_price_huf):'—'}</span></>:<span className="badge">Nincs beállítva</span>}</td><td>{f.cost===null?<span className="badge">Önköltség hiányzik</span>:<><strong>{formatHuf(f.margin??0)} / db</strong><br/><span className="muted">nettó fedezet {f.marginPct}% · önköltség {formatHuf(f.cost)}</span></>}</td><td><strong>{f.sold} db / 30 nap</strong><br/><span className="muted">előző 30 nap: {f.prev} db · {f.trend>=0?'+':''}{f.trend}%</span></td><td><span className="badge">{f.level==='critical'?'Kritikus':f.level==='warning'?'Figyelendő':'Rendben'}</span><br/><span className="muted">{f.days===null?'nincs fogyási adat':`kb. ${f.days} nap`}</span></td><td><strong>{f.orderQty?`+${f.orderQty} db`:'Nem szükséges'}</strong><br/><span className="muted">{f.purchaseCost===null&&f.orderQty?'tőkeigényhez add meg az önköltséget':f.orderQty?formatHuf(f.purchaseCost??0):`30 napos cél: ${f.target} db`}</span></td></tr>})}</tbody></table></div><p className="muted">A fedezet = nettó eladási ár − nettó egységköltség. Ez bruttó termékfedezet, nem vállalati nettó profit: marketing-, fizetési-, szállítási és egyéb működési költségeket nem von le.</p></section>}
