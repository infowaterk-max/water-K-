import Link from 'next/link';import{notFound}from'next/navigation';import{createAdminClient}from'@/lib/supabase/admin';import{CampaignActions}from'@/components/admin/campaign-actions';export const dynamic='force-dynamic';
type C={id:string;name:string;segment:string;template_key:string;status:string;scheduled_at:string|null;created_at:string;approved_at:string|null};type R={id:string;email:string;customer_name:string|null;orders_count:number;revenue_gross_huf:number;last_order_at:string|null;consent_ok:boolean;suppressed:boolean;eligible:boolean;exclusion_reason:string|null;communication_job_id:string|null};type E={id:string;action:string;note:string|null;created_at:string};type J={id:string;status:string;approved_at:string|null;sent_at:string|null;last_error:string|null};const sl:Record<string,string>={draft:'Piszkozat',review:'Ellenőrzésre vár',approved:'Jóváhagyva',queued:'Sorba téve',cancelled:'Törölve',completed:'Lezárva'};const reasons:Record<string,string>={NO_MARKETING_CONSENT:'Nincs aktív marketing-hozzájárulás',SUPPRESSED:'Tiltólistán szerepel',ELIGIBILITY_CHANGED_BEFORE_QUEUE:'Jogosultság megváltozott a sorba állítás előtt'};const al:Record<string,string>={submit_review:'Ellenőrzésre küldés',approve:'Jóváhagyás',queue:'Sorba állítás',cancel:'Törlés'};const jl:Record<string,string>={pending:'Várakozik',processing:'Feldolgozás',sent:'Elküldve',failed:'Hibás',blocked:'Blokkolva',cancelled:'Törölve'};
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const a=createAdminClient();const[{data:c},{data:rd},{data:ed}]=await Promise.all([a.from('marketing_campaigns').select('id,name,segment,template_key,status,scheduled_at,created_at,approved_at').eq('id',id).maybeSingle(),a.from('marketing_campaign_recipients').select('id,email,customer_name,orders_count,revenue_gross_huf,last_order_at,consent_ok,suppressed,eligible,exclusion_reason,communication_job_id').eq('campaign_id',id).order('eligible',{ascending:false}).order('revenue_gross_huf',{ascending:false}).limit(10000),a.from('marketing_campaign_events').select('id,action,note,created_at').eq('campaign_id',id).order('created_at',{ascending:false}).limit(100)]);if(!c)notFound();const campaign=c as C;const rows=(rd??[])as R[];const events=(ed??[])as E[];const jobIds=rows.map(r=>r.communication_job_id).filter((x):x is string=>Boolean(x));let jobs:J[]=[];if(jobIds.length){const{data}=await a.from('communication_jobs').select('id,status,approved_at,sent_at,last_error').in('id',jobIds);jobs=(data??[])as J[]}const jm=new Map(jobs.map(j=>[j.id,j]));const eligible=rows.filter(r=>r.eligible);const queued=jobIds.length;const approved=jobs.filter(j=>j.approved_at).length;const sent=jobs.filter(j=>j.status==='sent').length;const failed=jobs.filter(j=>j.status==='failed').length;const blocked=jobs.filter(j=>j.status==='blocked').length;const waiting=jobs.filter(j=>j.status==='pending'&&!j.approved_at).length;const deliveryRate=queued?Math.round(sent/queued*100):0;return <section className="adminMain"><Link href="/admin/kampanyok" className="textLink">← Kampányok</Link><span className="eyebrow">Kampány részletei</span><h1 className="sectionTitle">{campaign.name}</h1><div className="cards adminMetricCards"><div className="card"><span className="badge">Állapot</span><div className="price">{sl[campaign.status]??campaign.status}</div></div><div className="card"><span className="badge">Célcsoport</span><div className="price">{rows.length}</div><p className="muted">{eligible.length} jogosult</p></div><div className="card"><span className="badge">Sorba téve</span><div className="price">{queued}</div><p className="muted">{waiting} jóváhagyásra vár</p></div><div className="card"><span className="badge">Elküldve</span><div className="price">{sent}</div><p className="muted">{deliveryRate}% a sorba tettből</p></div></div><section className="card"><span className="eyebrow">Kampányeredmény</span><h2>Kommunikációs tölcsér</h2><div className="cards adminMetricCards"><div><strong>{queued}</strong><br/><span className="muted">Sorba tett</span></div><div><strong>{approved}</strong><br/><span className="muted">Jóváhagyott</span></div><div><strong>{sent}</strong><br/><span className="muted">Elküldött</span></div><div><strong>{blocked}</strong><br/><span className="muted">Blokkolt</span></div><div><strong>{failed}</strong><br/><span className="muted">Hibás</span></div></div></section><section className="card"><h2>Kampányműveletek</h2><p className="muted">Sablon: {campaign.template_key} · Időzítés: {campaign.scheduled_at?new Intl.DateTimeFormat('hu-HU',{dateStyle:'medium',timeStyle:'short'}).format(new Date(campaign.scheduled_at)):'azonnali a sorba állítás után'}</p><CampaignActions campaignId={campaign.id} status={campaign.status}/></section><section className="tableCard"><h2>Címzettek és kézbesítési állapot</h2><div className="adminTableScroll"><table className="adminTable"><thead><tr><th>Ügyfél</th><th>Rendelések</th><th>Korábbi forgalom</th><th>Hozzájárulás</th><th>Jogosultság</th><th>Kommunikáció</th><th>Ok / hiba</th></tr></thead><tbody>{rows.map(r=>{const j=r.communication_job_id?jm.get(r.communication_job_id):undefined;return <tr key={r.id}><td><strong>{r.customer_name||'—'}</strong><br/><span className="muted">{r.email}</span></td><td>{r.orders_count}</td><td>{new Intl.NumberFormat('hu-HU').format(r.revenue_gross_huf)} Ft</td><td>{r.consent_ok?'Aktív':'Nincs'}</td><td><span className="badge">{r.eligible?'Jogosult':'Kizárt'}</span></td><td>{j?<><span className="badge">{jl[j.status]??j.status}</span>{j.approved_at&&<><br/><span className="muted">Jóváhagyva</span></>}</>:<span className="muted">Nincs sorban</span>}</td><td><span className="muted">{j?.last_error??reasons[r.exclusion_reason??'']??r.exclusion_reason??'—'}</span></td></tr>})}</tbody></table></div></section><section className="card"><span className="eyebrow">Audit</span><h2>Kampánytörténet</h2><div className="integrationList">{events.map(e=><div key={e.id}><span><strong>{al[e.action]??e.action}</strong>{e.note&&<><br/><span className="muted">{e.note}</span></>}</span><span>{new Intl.DateTimeFormat('hu-HU',{dateStyle:'short',timeStyle:'short'}).format(new Date(e.created_at))}</span></div>)}</div>{events.length===0&&<p className="muted">Még nincs kampányművelet.</p>}</section></section>}
