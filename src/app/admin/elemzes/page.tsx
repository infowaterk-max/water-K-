import {formatHuf}from'@/lib/catalog';import{createAdminClient}from'@/lib/supabase/admin';const paidStatuses=['paid','processing','shipped','completed'];type Order={id:string;customer_id:string|null;customer_email:string;status:string;total_gross_huf:number;discount_gross_huf:number|null;coupon_code:string|null;created_at:string};type Item={order_id:string;sku:string;product_name:string;variant_label:string;quantity:number;line_total_gross_huf:number};export const dynamic='force-dynamic';
export default async function CommerceAnalytics(){const admin=createAdminClient();const[{data:od,error:oe},{data:id,error:ie},{data:pd}]=await Promise.all([admin.from('orders').select('id,customer_id,customer_email,status,total_gross_huf,discount_gross_huf,coupon_code,created_at').order('created_at',{ascending:false}).limit(5000),admin.from('order_items').select('order_id,sku,product_name,variant_label,quantity,line_total_gross_huf').limit(10000),admin.from('profiles').select('id,role,reseller_approved').limit(5000)]);const loadError=Boolean(oe||ie),orders=(od??[])as Order[],items=(id??[])as Item[],paid=orders.filter(o=>paidStatuses.includes(o.status)),paidIds=new Set(paid.map(o=>o.id)),paidItems=items.filter(i=>paidIds.has(i.order_id)),roles=new Map((pd??[]).map(p=>[p.id,p.role==='reseller'&&p.reseller_approved?'reseller':'customer'])),retail=paid.filter(o=>!o.customer_id||roles.get(o.customer_id)!=='reseller'),reseller=paid.filter(o=>o.customer_id&&roles.get(o.customer_id)==='reseller');const revenue=(r:Order[])=>r.reduce((s,o)=>s+Number(o.total_gross_huf||0),0),totalRevenue=revenue(paid),discount=paid.reduce((s,o)=>s+Number(o.discount_gross_huf??0),0),now=Date.now(),day=86400000;const current30=paid.filter(o=>+new Date(o.created_at)>=now-30*day),previous30=paid.filter(o=>+new Date(o.created_at)>=now-60*day&&+new Date(o.created_at)<now-30*day),r30=revenue(current30),rPrev=revenue(previous30),growth=rPrev?Math.round((r30-rPrev)/rPrev*100):r30?100:0;
 const customers=new Map<string,{orders:number;revenue:number;last:number}>();for(const o of paid){const key=o.customer_id??o.customer_email.toLowerCase(),c=customers.get(key)??{orders:0,revenue:0,last:0};c.orders++;c.revenue+=Number(o.total_gross_huf||0);c.last=Math.max(c.last,+new Date(o.created_at));customers.set(key,c)}const vals=[...customers.values()],repeat=vals.filter(c=>c.orders>1),repeatRevenue=repeat.reduce((s,c)=>s+c.revenue,0),repeatRate=customers.size?Math.round(repeat.length/customers.size*100):0,avgCustomer=customers.size?Math.round(totalRevenue/customers.size):0,highValue=vals.filter(c=>c.revenue>=50000).length,active90=vals.filter(c=>c.last>=now-90*day).length;
 const productMap=new Map<string,{name:string;qty:number;revenue:number}>();for(const i of paidItems){const c=productMap.get(i.sku)??{name:`${i.product_name} · ${i.variant_label}`,qty:0,revenue:0};c.qty+=i.quantity;c.revenue+=Number(i.line_total_gross_huf||0);productMap.set(i.sku,c)}const products=[...productMap.entries()].map(([sku,v])=>({sku,...v})).sort((a,b)=>b.revenue-a.revenue);const couponMap=new Map<string,{orders:number;discount:number;revenue:number}>();for(const o of paid.filter(o=>o.coupon_code)){const code=o.coupon_code!,c=couponMap.get(code)??{orders:0,discount:0,revenue:0};c.orders++;c.discount+=Number(o.discount_gross_huf??0);c.revenue+=Number(o.total_gross_huf||0);couponMap.set(code,c)}const coupons=[...couponMap.entries()].map(([code,v])=>({code,...v})).sort((a,b)=>b.revenue-a.revenue);const months=new Map<string,{orders:number;revenue:number}>();for(const o of paid){const d=new Date(o.created_at),key=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`,c=months.get(key)??{orders:0,revenue:0};c.orders++;c.revenue+=Number(o.total_gross_huf||0);months.set(key,c)}const monthly=[...months.entries()].sort((a,b)=>b[0].localeCompare(a[0])).slice(0,12);const avg=paid.length?Math.round(totalRevenue/paid.length):0;
 return <section className="adminMain"><span className="eyebrow">Admin · Elemzés</span><h1 className="sectionTitle">Értékesítési intelligencia</h1><p className="lead">Forgalmi trend, ügyfélérték, termék-, partner- és kupon teljesítmény a ténylegesen fizetett rendelésekből.</p>{loadError&&<div className="errorNotice"><strong>Az elemzés egy része most nem tölthető be.</strong></div>}<div className="cards adminMetricCards"><div className="card"><span className="badge">Utolsó 30 nap</span><div className="price">{formatHuf(r30)}</div><p className="muted">{growth>=0?'+':''}{growth}% az előző 30 naphoz képest</p></div><div className="card"><span className="badge">Ügyfélérték</span><div className="price">{formatHuf(avgCustomer)}</div><p className="muted">átlagos történeti bevétel / fizető ügyfél</p></div><div className="card"><span className="badge">Visszatérők</span><div className="price">{repeatRate}%</div><p className="muted">{repeat.length} ügyfél · {formatHuf(repeatRevenue)}</p></div><div className="card"><span className="badge">Aktív ügyfélbázis</span><div className="price">{active90}</div><p className="muted">90 napon belül vásárolt · {highValue} ügyfél 50 000 Ft+ értékkel</p></div></div><div className="splitFeature"><section className="featurePanel"><span className="eyebrow">Csatornák</span><h2>Lakossági vs. viszonteladói</h2><div className="integrationList"><div><span>Lakossági forgalom</span><strong>{formatHuf(revenue(retail))}</strong></div><div><span>Lakossági rendelések</span><strong>{retail.length}</strong></div><div><span>Viszonteladói forgalom</span><strong>{formatHuf(revenue(reseller))}</strong></div><div><span>Viszonteladói rendelések</span><strong>{reseller.length}</strong></div></div></section><section className="featurePanel darkPanel"><span className="eyebrow">Összesített teljesítmény</span><h2>{formatHuf(totalRevenue)}</h2><ul className="featureList"><li>{paid.length} fizetett rendelés</li><li>{formatHuf(avg)} átlagos kosárérték</li><li>{formatHuf(discount)} összes adott kedvezmény</li></ul></section></div><section className="card"><span className="eyebrow">12 havi trend</span><h2>Havi fizetett forgalom</h2><div className="adminTableScroll"><table className="adminTable"><thead><tr><th>Hónap</th><th>Rendelések</th><th>Forgalom</th><th>Átlagos kosár</th></tr></thead><tbody>{monthly.map(([m,v])=><tr key={m}><td><strong>{m}</strong></td><td>{v.orders}</td><td><strong>{formatHuf(v.revenue)}</strong></td><td>{formatHuf(Math.round(v.revenue/v.orders))}</td></tr>)}</tbody></table></div></section><section className="card"><span className="eyebrow">Termékteljesítmény</span><h2>Mi termeli a forgalmat?</h2><div className="adminTableScroll"><table className="adminTable"><thead><tr><th>Termék / SKU</th><th>Eladott mennyiség</th><th>Árbevétel</th><th>Részesedés</th></tr></thead><tbody>{products.map(p=><tr key={p.sku}><td><strong>{p.name}</strong><br/><span className="muted">{p.sku}</span></td><td>{p.qty} db</td><td><strong>{formatHuf(p.revenue)}</strong></td><td>{totalRevenue?Math.round(p.revenue/totalRevenue*100):0}%</td></tr>)}</tbody></table></div></section><section className="card"><span className="eyebrow">Kuponhatás</span><h2>Kedvezmények üzleti eredménye</h2><div className="adminTableScroll"><table className="adminTable"><thead><tr><th>Kupon</th><th>Rendelés</th><th>Kedvezmény</th><th>Fizetett forgalom</th></tr></thead><tbody>{coupons.map(c=><tr key={c.code}><td><strong>{c.code}</strong></td><td>{c.orders}</td><td>{formatHuf(c.discount)}</td><td><strong>{formatHuf(c.revenue)}</strong></td></tr>)}</tbody></table></div></section></section>}
