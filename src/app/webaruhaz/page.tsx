import Link from 'next/link';
import { AddToCart } from '@/components/catalog/add-to-cart';
import { formatHuf, products } from '@/lib/catalog';
export default function Shop(){return <main className="section"><div className="shell"><span className="eyebrow">Water-K webáruház</span><h1 className="sectionTitle">Találd meg a megfelelő kiszerelést</h1><p className="lead">Átlátható kínálat, gyors vásárlás, közvetlenül a Water-K rendszeréből.</p><div className="cards">{products.map(p=><article className="card" key={p.slug}><span className="badge">{p.stock>0?'Raktáron':'Elfogyott'}</span><h2>{p.name}</h2><p className="muted">{p.short}</p><div className="price">{formatHuf(p.grossPrice)}</div><div className="actions"><AddToCart id={p.id} name={p.name} price={p.grossPrice}/><Link className="btn" href={`/termek/${p.slug}`}>Részletek</Link></div></article>)}</div></div></main>}
