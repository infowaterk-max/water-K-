import{NextResponse}from'next/server';
import{z}from'zod';
import{createClient}from'@/lib/supabase/server';
import{createAdminClient}from'@/lib/supabase/admin';
import{getCurrentWebshopInstance}from'@/lib/instances/access';
const key=z.string().trim().regex(/^[a-z0-9]+(?:[._-][a-z0-9]+)*$/);const group=z.object({type:z.enum(['composition','configuration']),id:key,engineVersion:z.string().trim().min(1).max(160),definitionKey:key,definitionVersion:z.number().int().min(1).max(1000),itemKey:z.string().trim().regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/),slotId:key.nullable().optional()});
const schema=z.object({items:z.array(z.object({productId:z.string().uuid(),quantity:z.number().int().positive().max(99),lineId:z.string().trim().min(1).max(240).optional(),commerceGroup:group.optional()})).min(1).max(30),checkout:z.record(z.string(),z.unknown()).optional().default({})});
export async function POST(request:Request){const s=await createClient(),{data:{user}}=await s.auth.getUser();if(!user?.email)return NextResponse.json({ok:true,stored:false,reason:'guest'});const instance=await getCurrentWebshopInstance();if(!instance)return NextResponse.json({error:'Nincs aktív webshop.'},{status:409});let raw:unknown;try{raw=await request.json()}catch{return NextResponse.json({error:'Érvénytelen kérés.'},{status:400})}const parsed=schema.safeParse(raw);if(!parsed.success)return NextResponse.json({error:'Érvénytelen kosáradat.'},{status:400});const a=createAdminClient();const{data,error}=await a.rpc('upsert_checkout_recovery_intent_v2',{p_instance_id:instance.id,p_user_id:user.id,p_email:user.email,p_cart:parsed.data.items,p_checkout:parsed.data.checkout});if(error)return NextResponse.json({error:'A kosármentés most nem sikerült.'},{status:500});const recovery=(data??{})as{id?:string;token?:string;expiresAt?:string};if(!recovery.id)return NextResponse.json({error:'A kosármentés eredménye nem igazolható.'},{status:500});return NextResponse.json({ok:true,stored:true,recovery});}
