import{NextResponse}from'next/server';import{z}from'zod';import{createClient}from'@/lib/supabase/server';
const schema=z.object({orderId:z.string().uuid(),reason:z.enum(['elallas','serult','hibas','teves_termek','egyeb']),note:z.string().trim().max(2000).optional().default('')});
export async function POST(request:Request){const s=await createClient(),{data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Bejelentkezés szükséges.'},{status:401});let raw:unknown;try{raw=await request.json()}catch{return NextResponse.json({error:'Érvénytelen kérés.'},{status:400})}const parsed=schema.safeParse(raw);if(!parsed.success)return NextResponse.json({error:'Érvénytelen kérelem.'},{status:400});const{data:order,error:oe}=await s.from('orders').select('id,customer_email,status,created_at').eq('id',parsed.data.orderId).eq('customer_id',user.id).maybeSingle();if(oe||!order)return NextResponse.json({error:'A rendelés nem található.'},{status:404});if(!['shipped','completed'].includes(order.status))return NextResponse.json({error:'Ehhez a rendeléshez jelenleg nem indítható visszaküldési kérelem.'},{status:409});const{data:existing}=await s.from('return_cases').select('id,status').eq('order_id',order.id).eq('user_id',user.id).in('status',['requested','approved','received','refund_pending']).limit(1).maybeSingle();if(existing)return NextResponse.json({error:'Ehhez a rendeléshez már van folyamatban lévő ügy.'},{status:409});const{data,error}=await s.from('return_cases').insert({order_id:order.id,user_id:user.id,customer_email:order.customer_email,reason:parsed.data.reason,customer_note:parsed.data.note||null}).select('id').single();if(error||!data)return NextResponse.json({error:'A kérelem rögzítése nem sikerült.'},{status:500});return NextResponse.json({ok:true,id:data.id})}
