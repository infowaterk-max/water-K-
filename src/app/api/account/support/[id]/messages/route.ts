import{NextResponse}from'next/server';import{z}from'zod';import{createClient}from'@/lib/supabase/server';const schema=z.object({message:z.string().trim().min(2).max(4000)});export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const s=await createClient(),{data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Bejelentkezés szükséges.'},{status:401});const{id}=await params;if(!z.string().uuid().safeParse(id).success)return NextResponse.json({error:'Érvénytelen ügyazonosító.'},{status:400});let raw:unknown;try{raw=await request.json()}catch{return NextResponse.json({error:'Érvénytelen kérés.'},{status:400})}const parsed=schema.safeParse(raw);if(!parsed.success)return NextResponse.json({error:'Az üzenet 2–4000 karakter lehet.'},{status:400});const{data:ticket,error:te}=await s.from('support_tickets').select('id,status').eq('id',id).eq('user_id',user.id).maybeSingle();if(te||!ticket)return NextResponse.json({error:'Az ügy nem található.'},{status:404});if(ticket.status==='closed')return NextResponse.json({error:'A lezárt ügyhöz nem küldhető új válasz.'},{status:409});const{error}=await s.from('support_ticket_messages').insert({ticket_id:id,author_user_id:user.id,author_role:'customer',message:parsed.data.message});if(error)return NextResponse.json({error:'A válasz rögzítése nem sikerült.'},{status:500});await s.from('support_tickets').update({status:'open',updated_at:new Date().toISOString()}).eq('id',id).eq('user_id',user.id);return NextResponse.json({ok:true})}
