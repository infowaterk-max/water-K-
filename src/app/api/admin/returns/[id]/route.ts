import{NextResponse}from'next/server';import{z}from'zod';import{getAdminRequestUser}from'@/lib/auth/admin-api';import{createAdminClient}from'@/lib/supabase/admin';import{recordAdminAudit}from'@/lib/admin/audit';
const schema=z.object({status:z.enum(['requested','approved','rejected','received','refund_pending','refunded','closed']),refundAmount:z.union([z.number().int().min(0).max(10000000),z.null()]).optional(),refundReference:z.union([z.string().trim().max(200),z.null()]).optional(),adminNote:z.union([z.string().trim().max(2000),z.null()]).optional()});
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const actor=await getAdminRequestUser();if(!actor)return NextResponse.json({error:'Nincs jogosultság.'},{status:403});const{id}=await params;if(!z.string().uuid().safeParse(id).success)return NextResponse.json({error:'Érvénytelen ügyazonosító.'},{status:400});let raw:unknown;try{raw=await request.json()}catch{return NextResponse.json({error:'Érvénytelen kérés.'},{status:400})}const parsed=schema.safeParse(raw);if(!parsed.success)return NextResponse.json({error:'Érvénytelen ügyadat.'},{status:400});const a=createAdminClient(),{data:current,error:ce}=await a.from('return_cases').select('*').eq('id',id).maybeSingle();if(ce||!current)return NextResponse.json({error:'Az ügy nem található.'},{status:404});const now=new Date().toISOString(),update:Record<string,unknown>={status:parsed.data.status,updated_at:now};if(parsed.data.refundAmount!==undefined)update.refund_amount_gross_huf=parsed.data.refundAmount;if(parsed.data.refundReference!==undefined)update.refund_reference=parsed.data.refundReference;if(parsed.data.adminNote!==undefined)update.admin_note=parsed.data.adminNote;if(parsed.data.status==='approved'&&!current.approved_at)update.approved_at=now;if(parsed.data.status==='received'&&!current.received_at)update.received_at=now;if(parsed.data.status==='refunded'){if((parsed.data.refundAmount??current.refund_amount_gross_huf)==null)return NextResponse.json({error:'A visszatérített állapothoz add meg a visszatérítés összegét.'},{status:409});update.refunded_at=current.refunded_at??now}if(parsed.data.status==='closed')update.closed_at=current.closed_at??now;const{data:updated,error}=await a.from('return_cases').update(update).eq('id',id).select('*').single();if(error)return NextResponse.json({error:'Az ügy módosítása nem sikerült.'},{status:500});if(parsed.data.status==='refunded'){const{data:open}=await a.from('return_cases').select('refund_amount_gross_huf').eq('order_id',current.order_id).eq('status','refunded');const refunded=(open??[]).reduce((s,x)=>s+Number(x.refund_amount_gross_huf||0),0),{data:order}=await a.from('orders').select('total_gross_huf,status').eq('id',current.order_id).maybeSingle();if(order&&refunded>=Number(order.total_gross_huf||0)&&order.status!=='refunded')await a.from('orders').update({status:'refunded'}).eq('id',current.order_id)}await recordAdminAudit({actorUserId:actor.id,action:'returns.case_updated',entityType:'return_case',entityId:id,summary:`Visszáru ügy: ${parsed.data.status}`,beforeState:current,afterState:updated});return NextResponse.json({ok:true})}
