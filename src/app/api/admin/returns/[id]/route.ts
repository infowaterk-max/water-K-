import{NextResponse}from'next/server';import{z}from'zod';import{getAdminRequestUser}from'@/lib/auth/admin-api';import{createAdminClient}from'@/lib/supabase/admin';import{recordAdminAudit}from'@/lib/admin/audit';
const statuses=['requested','approved','rejected','received','refund_pending','refunded','closed']as const;const schema=z.object({status:z.enum(statuses),refundAmount:z.union([z.number().int().min(0).max(10000000),z.null()]).optional(),refundReference:z.union([z.string().trim().max(200),z.null()]).optional(),adminNote:z.union([z.string().trim().max(2000),z.null()]).optional(),restock:z.boolean().optional().default(false)});
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const actor=await getAdminRequestUser();if(!actor)return NextResponse.json({error:'Nincs jogosultság.'},{status:403});const{id}=await params;if(!z.string().uuid().safeParse(id).success)return NextResponse.json({error:'Érvénytelen ügyazonosító.'},{status:400});let raw:unknown;try{raw=await request.json()}catch{return NextResponse.json({error:'Érvénytelen kérés.'},{status:400})}const parsed=schema.safeParse(raw);if(!parsed.success)return NextResponse.json({error:'Érvénytelen ügyadat.'},{status:400});const a=createAdminClient(),{data:current,error:ce}=await a.from('return_cases').select('*').eq('id',id).maybeSingle();if(ce||!current)return NextResponse.json({error:'Az ügy nem található.'},{status:404});const effectiveAmount=parsed.data.refundAmount!==undefined?parsed.data.refundAmount:current.refund_amount_gross_huf,effectiveReference=parsed.data.refundReference!==undefined?parsed.data.refundReference:current.refund_reference,effectiveNote=parsed.data.adminNote!==undefined?parsed.data.adminNote:current.admin_note;const{data:transition,error}=await a.rpc('transition_return_case',{p_case_id:id,p_actor:actor.id,p_target_status:parsed.data.status,p_refund_amount:effectiveAmount,p_refund_reference:effectiveReference??'',p_admin_note:effectiveNote??'',p_restock:parsed.data.restock});if(error){const message=error.message||'Az ügy módosítása nem sikerült.';const conflict=/állapotváltás|visszatérítés|készletre|vissza lett állítva|nem található/i.test(message);return NextResponse.json({error:message},{status:conflict?409:500})}const{data:updated}=await a.from('return_cases').select('*').eq('id',id).maybeSingle();await recordAdminAudit({actorUserId:actor.id,action:'returns.case_updated',entityType:'return_case',entityId:id,summary:`Visszáru ügy: ${parsed.data.status}${parsed.data.restock?' · készlet visszaállítva':''}`,beforeState:current,afterState:updated??transition});return NextResponse.json({ok:true,transition})}
