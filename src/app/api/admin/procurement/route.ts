import{NextResponse}from'next/server';import{z}from'zod';import{getAdminRequestUser}from'@/lib/auth/admin-api';import{createAdminClient}from'@/lib/supabase/admin';import{recordAdminAudit}from'@/lib/admin/audit';import{hasCurrentPlanFeature}from'@/lib/plans/access';
const item=z.object({variantId:z.string().uuid(),quantity:z.number().int().min(1).max(100000),unitCostNetHuf:z.number().min(0).max(10000000)});const schema=z.object({supplierName:z.string().trim().min(2).max(120),expectedAt:z.string().optional(),paymentTermsDays:z.number().int().min(0).max(365).default(8),notes:z.string().trim().max(1000).optional(),items:z.array(item).min(1).max(100)}).superRefine((d,c)=>{const ids=new Set<string>();for(const x of d.items){if(ids.has(x.variantId))c.addIssue({code:z.ZodIssueCode.custom,message:'Ugyanaz a termék csak egyszer szerepelhet a beszerzésben.',path:['items']});ids.add(x.variantId)}});
export async function POST(request:Request){const actor=await getAdminRequestUser();if(!actor)return NextResponse.json({error:'Nincs jogosultság.'},{status:403});if(!(await hasCurrentPlanFeature('procurement')))return NextResponse.json({error:'A beszerzés a Pro csomag része.'},{status:403});let raw:unknown;try{raw=await request.json()}catch{return NextResponse.json({error:'Érvénytelen kérés.'},{status:400})}const parsed=schema.safeParse(raw);if(!parsed.success)return NextResponse.json({error:parsed.error.issues[0]?.message||'Érvénytelen beszerzési adat.'},{status:400});const a=createAdminClient(),d=parsed.data,expected=d.expectedAt?new Date(`${d.expectedAt}T12:00:00Z`):null;if(expected&&Number.isNaN(+expected))return NextResponse.json({error:'Érvénytelen várható érkezési dátum.'},{status:400});const paymentDue=expected?new Date(+expected+d.paymentTermsDays*86400000):new Date(Date.now()+d.paymentTermsDays*86400000),orderNumber=`PO-${new Date().toISOString().slice(0,10).replaceAll('-','')}-${crypto.randomUUID().slice(0,8).toUpperCase()}`;const{data:created,error}=await a.rpc('create_purchase_order',{p_order_number:orderNumber,p_supplier_name:d.supplierName,p_payment_terms_days:d.paymentTermsDays,p_expected_at:expected?.toISOString().slice(0,10)??null,p_payment_due_at:paymentDue.toISOString().slice(0,10),p_notes:d.notes??null,p_created_by:actor.id,p_items:d.items});if(error||!created)return NextResponse.json({error:error?.message||'A beszerzési rendelés nem hozható létre.'},{status:500});const result=created as{ id:string;supplierId:string;supplierName:string;netTotal:number};await recordAdminAudit({actorUserId:actor.id,action:'procurement.purchase_order_created',entityType:'purchase_order',entityId:result.id,summary:`${orderNumber} beszerzési rendelés létrehozva`,afterState:{supplier:result.supplierName,netTotal:Number(result.netTotal||0),items:d.items.length,expectedAt:d.expectedAt??null},metadata:{orderNumber,supplierId:result.supplierId}});return NextResponse.json({ok:true,id:result.id,orderNumber})}
