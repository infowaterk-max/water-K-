import { NextResponse } from 'next/server';
import { z } from 'zod';
import { getAdminRequestUser } from '@/lib/auth/admin-api';
import { createAdminClient } from '@/lib/supabase/admin';
import { recordAdminAudit } from '@/lib/admin/audit';
const schema=z.object({priority:z.number().int().min(0).max(10000).optional(),active:z.boolean().optional(),headline:z.string().trim().max(120).nullable().optional()}).refine(v=>Object.keys(v).length>0,'Nincs módosítás.');
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const actor=await getAdminRequestUser();if(!actor)return NextResponse.json({error:'Nincs jogosultság.'},{status:403});const{id}=await params;if(!z.string().uuid().safeParse(id).success)return NextResponse.json({error:'Érvénytelen azonosító.'},{status:400});let raw:unknown;try{raw=await request.json()}catch{return NextResponse.json({error:'Érvénytelen kérés.'},{status:400})}const parsed=schema.safeParse(raw);if(!parsed.success)return NextResponse.json({error:'Érvénytelen módosítás.'},{status:400});const admin=createAdminClient(),{data:before}=await admin.from('product_recommendation_rules').select('*').eq('id',id).maybeSingle();if(!before)return NextResponse.json({error:'A szabály nem található.'},{status:404});const update:Record<string,unknown>={updated_at:new Date().toISOString()};if(parsed.data.priority!==undefined)update.priority=parsed.data.priority;if(parsed.data.active!==undefined)update.active=parsed.data.active;if(parsed.data.headline!==undefined)update.headline=parsed.data.headline;const{data:after,error}=await admin.from('product_recommendation_rules').update(update).eq('id',id).select('*').single();if(error)return NextResponse.json({error:'A módosítás nem sikerült.'},{status:500});await recordAdminAudit({actorUserId:actor.id,action:'catalog.recommendation_updated',entityType:'product_recommendation_rule',entityId:id,summary:'Termékajánlási szabály módosítva',beforeState:before,afterState:after});return NextResponse.json({ok:true})}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const actor=await getAdminRequestUser();if(!actor)return NextResponse.json({error:'Nincs jogosultság.'},{status:403});const{id}=await params;if(!z.string().uuid().safeParse(id).success)return NextResponse.json({error:'Érvénytelen azonosító.'},{status:400});const admin=createAdminClient(),{data:before}=await admin.from('product_recommendation_rules').select('*').eq('id',id).maybeSingle();if(!before)return NextResponse.json({error:'A szabály nem található.'},{status:404});const{error}=await admin.from('product_recommendation_rules').delete().eq('id',id);if(error)return NextResponse.json({error:'A törlés nem sikerült.'},{status:500});await recordAdminAudit({actorUserId:actor.id,action:'catalog.recommendation_deleted',entityType:'product_recommendation_rule',entityId:id,summary:'Termékajánlási szabály törölve',beforeState:before});return NextResponse.json({ok:true})}
