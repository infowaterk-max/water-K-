import{NextRequest,NextResponse}from'next/server';import{getAdminRequestUser}from'@/lib/auth/admin-api';import{createAdminClient}from'@/lib/supabase/admin';import{hasCurrentPlanFeature}from'@/lib/plans/access';export async function POST(req:NextRequest){const user=await getAdminRequestUser();if(!user)return NextResponse.json({error:'Nincs jogosultság.'},{status:403});if(!(await hasCurrentPlanFeature('automation')))return NextResponse.json({error:'Az automatizálás a Pro csomag része.'},{status:403});let body:{paused?:boolean;reason?:string};try{body=await req.json()}catch{return NextResponse.json({error:'Érvénytelen kérés.'},{status:400})}if(typeof body.paused!=='boolean')return NextResponse.json({error:'Érvénytelen állapot.'},{status:400});const a=createAdminClient();const{data,error}=await a.rpc('set_automation_global_pause',{p_actor_id:user.id,p_paused:body.paused,p_reason:String(body.reason??''),p_event_key:`global:${user.id}:${body.paused}:${Date.now()}`});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true,data})}
