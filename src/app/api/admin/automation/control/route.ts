import{NextRequest,NextResponse}from'next/server';import{getAdminRequestUser}from'@/lib/auth/admin-api';import{getPlatformRole}from'@/lib/auth/platform-operator';import{createAdminClient}from'@/lib/supabase/admin';export async function POST(req:NextRequest){const[user,platformRole]=await Promise.all([getAdminRequestUser(),getPlatformRole()]);if(!user||!platformRole)return NextResponse.json({error:'A globális automatizálási vezérlés csak a Platform Workbenchből érhető el.'},{status:403});let body:{paused?:boolean;reason?:string};try{body=await req.json()}catch{return NextResponse.json({error:'Érvénytelen kérés.'},{status:400})}if(typeof body.paused!=='boolean')return NextResponse.json({error:'Érvénytelen állapot.'},{status:400});const a=createAdminClient();const{data,error}=await a.rpc('set_automation_global_pause',{p_actor_id:user.id,p_paused:body.paused,p_reason:String(body.reason??''),p_event_key:`platform-global:${user.id}:${body.paused}:${Date.now()}`});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true,data})}
