import{NextRequest,NextResponse}from'next/server';import{getAdminRequestUser}from'@/lib/auth/admin-api';import{createAdminClient}from'@/lib/supabase/admin';import{hasCurrentPlanFeature}from'@/lib/plans/access';
export async function POST(req:NextRequest){const user=await getAdminRequestUser();if(!user)return NextResponse.json({error:'Nincs jogosultság.'},{status:403});if(!(await hasCurrentPlanFeature('executiveAnalytics')))return NextResponse.json({error:'Az Irányítóközpont a Pro csomag része.'},{status:403});let body:{taskId?:string;targetStatus?:string;outcome?:string};try{body=await req.json()}catch{return NextResponse.json({error:'Érvénytelen kérés.'},{status:400})}const taskId=String(body.taskId??''),targetStatus=String(body.targetStatus??'');if(!taskId||!['in_progress','completed','cancelled'].includes(targetStatus))return NextResponse.json({error:'Érvénytelen művelet.'},{status:400});const outcome=String(body.outcome??'').trim();if(targetStatus==='completed'&&!outcome)return NextResponse.json({error:'A lezáráshoz rövid eredmény szükséges.'},{status:400});const a=createAdminClient();const{data,error}=await a.rpc('transition_control_task',{p_task_id:taskId,p_target_status:targetStatus,p_event_key:`admin-task:${taskId}:${targetStatus}:${crypto.randomUUID()}`,p_actor_id:user.id,p_outcome:outcome});if(error)return NextResponse.json({error:error.message||'A kontrollfeladat módosítása nem sikerült.'},{status:409});return NextResponse.json({ok:true,task:data});}
