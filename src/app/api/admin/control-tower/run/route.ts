import{NextResponse}from'next/server';import{getAdminRequestUser}from'@/lib/auth/admin-api';import{hasCurrentPlanFeature}from'@/lib/plans/access';import{requireCurrentStoreContext}from'@/lib/instances/scope';import{createAdminClient}from'@/lib/supabase/admin';export async function POST(){const user=await getAdminRequestUser('store.manage');if(!user)return NextResponse.json({error:'Nincs jogosultság.'},{status:403});const scope=await requireCurrentStoreContext('store.manage');if(!(await hasCurrentPlanFeature('executiveAnalytics')))return NextResponse.json({error:'Az Irányítóközpont a Pro csomag része.'},{status:403});const a=createAdminClient(),runKey=`admin:${user.id}:${crypto.randomUUID()}`;const{data,error}=await a.rpc('process_control_tower_cycle_v2',{p_instance_id:scope.instanceId,p_run_key:runKey});if(error)return NextResponse.json({error:error.message||'A kontrollciklus nem sikerült.'},{status:500});return NextResponse.json({ok:true,run:data})}
