import { NextResponse } from 'next/server';
import { z } from 'zod';
import { createClient } from '@/lib/supabase/server';
import { createAdminClient } from '@/lib/supabase/admin';
import { enqueueIntegrationJob } from '@/lib/integrations/outbox';
import { getCommerceSettings } from '@/lib/commerce/settings';
import { getPaymentGatewayAdapter,getShippingProviderAdapter } from '@/lib/integrations/adapters';
import { getCommunicationIdentity } from '@/lib/communication/identity';

const providerCode=z.string().trim().regex(/^[a-z0-9_-]{2,80}$/);
const checkoutSchema=z.object({customerType:z.enum(['retail','company','reseller']),email:z.string().trim().email(),name:z.string().trim().min(2).max(160),phone:z.string().trim().min(5).max(40),companyName:z.string().trim().max(200).optional(),taxNumber:z.string().trim().max(40).optional(),billingPostcode:z.string().trim().min(2).max(20),billingCity:z.string().trim().min(2).max(120),billingAddress:z.string().trim().min(2).max(300),sameAddress:z.enum(['true','false']).default('true'),shippingPostcode:z.string().trim().max(20).optional(),shippingCity:z.string().trim().max(120).optional(),shippingAddress:z.string().trim().max(300).optional(),shippingProvider:providerCode,shippingKind:z.string().optional(),paymentProvider:providerCode,parcelPointId:z.string().trim().max(160).optional(),note:z.string().trim().max(1000).optional(),couponCode:z.string().trim().max(32).optional(),legalAccepted:z.literal('true')});
const schema=z.object({checkout:checkoutSchema,items:z.array(z.object({productId:z.string().uuid(),quantity:z.number().int().positive().max(99)})).min(1).max(30)});type PlaceOrderResult={order_id:string;order_number:string;subtotal_gross_huf:number;discount_gross_huf:number;shipping_gross_huf:number;total_gross_huf:number;coupon_code:string|null;idempotency_replayed?:boolean};
export async function POST(request:Request){const idempotencyKey=(request.headers.get('x-idempotency-key')??'').trim();if(idempotencyKey.length<16||idempotencyKey.length>120)return NextResponse.json({error:'Hiányzó vagy érvénytelen rendelési kérésazonosító.'},{status:400});let body:unknown;try{body=await request.json()}catch{return NextResponse.json({error:'Érvénytelen JSON kérés.'},{status:400})}const parsed=schema.safeParse(body);if(!parsed.success)return NextResponse.json({error:'Hiányos vagy érvénytelen rendelési adatok.'},{status:400});const{checkout,items}=parsed.data,commerce=await getCommerceSettings(),shipping=commerce.shippingOptions.find(o=>o.code===checkout.shippingProvider),payment=commerce.paymentOptions.find(o=>o.code===checkout.paymentProvider);if(!shipping)return NextResponse.json({error:'Ez a szállítási szolgáltató ebben a webáruházban nem aktív.'},{status:409});if(!payment)return NextResponse.json({error:'Ez a fizetési szolgáltató ebben a webáruházban nem aktív.'},{status:409});if(checkout.customerType!=='retail'&&(!checkout.companyName||!checkout.taxNumber))return NextResponse.json({error:'Céges vagy viszonteladói rendeléshez cégnév és adószám szükséges.'},{status:400});if(shipping.kind==='parcel_point'){if(!checkout.parcelPointId)return NextResponse.json({error:'Ehhez a szállítási módhoz átvételi pontot kell választani.'},{status:400});try{const shippingAdapter=getShippingProviderAdapter(shipping.adapterKey);if(!shippingAdapter.validatePickupPoint)return NextResponse.json({error:'A kiválasztott csomagpont szerveroldali ellenőrzése ehhez a szolgáltatóhoz még nincs aktiválva.'},{status:409});const valid=await shippingAdapter.validatePickupPoint(checkout.parcelPointId);if(!valid)return NextResponse.json({error:'A kiválasztott átvételi pont már nem érvényes. Kérlek válassz másikat.'},{status:409})}catch(error){return NextResponse.json({error:error instanceof Error?`A csomagpont ellenőrzése nem sikerült: ${error.message}`:'A csomagpont ellenőrzése átmenetileg nem érhető el.'},{status:503})}}const homeDelivery=shipping.kind==='home_delivery';if(homeDelivery&&checkout.sameAddress==='false'&&(!checkout.shippingPostcode||!checkout.shippingCity||!checkout.shippingAddress))return NextResponse.json({error:'A szállítási cím hiányos.'},{status:400});const shippingPostcode=homeDelivery&&checkout.sameAddress==='false'?checkout.shippingPostcode??'':checkout.billingPostcode,shippingCity=homeDelivery&&checkout.sameAddress==='false'?checkout.shippingCity??'':checkout.billingCity,shippingAddress=homeDelivery&&checkout.sameAddress==='false'?checkout.shippingAddress??'':checkout.billingAddress;
try{const sessionClient=await createClient();const{data:{user}}=await sessionClient.auth.getUser();const admin=createAdminClient();const{data,error}=await admin.rpc('place_order_provider_v2_idempotent',{p_idempotency_key:idempotencyKey,p_customer_email:checkout.email,p_billing_name:checkout.name,p_billing_company:checkout.companyName??'',p_billing_tax_number:checkout.taxNumber??'',p_billing_postcode:checkout.billingPostcode,p_billing_city:checkout.billingCity,p_billing_address:checkout.billingAddress,p_shipping_name:checkout.name,p_shipping_postcode:shippingPostcode,p_shipping_city:shippingCity,p_shipping_address:shippingAddress,p_customer_phone:checkout.phone,p_shipping_provider:shipping.code,p_shipping_kind:shipping.kind,p_shipping_fee_huf:shipping.fee,p_free_shipping_threshold_huf:commerce.freeShippingThreshold,p_parcel_point_id:checkout.parcelPointId??'',p_payment_provider:payment.code,p_note:checkout.note??'',p_customer_id:user?.id??null,p_coupon_code:(checkout.couponCode??'').toUpperCase(),p_items:items.map(item=>({variant_id:item.productId,quantity:item.quantity}))});if(error||!data)return NextResponse.json({error:error?.message??'A rendelés mentése nem sikerült.'},{status:409});const order=data as PlaceOrderResult,replayed=order.idempotency_replayed===true;if(user?.id)await admin.rpc('convert_checkout_recovery_intent',{p_user_id:user.id,p_order_id:order.order_id});const{data:confirmation}=await admin.from('orders').select('confirmation_token').eq('id',order.order_id).maybeSingle();if(!confirmation?.confirmation_token)return NextResponse.json({error:'A rendelés rögzült, de a visszaigazolás előkészítése nem sikerült.'},{status:503});if(!replayed){await admin.from('order_events').insert({order_id:order.order_id,event_type:'legal_terms_accepted',actor_user_id:user?.id??null,metadata:{accepted_at:new Date().toISOString(),terms_path:'/aszf',privacy_path:'/adatvedelem',idempotency_key:idempotencyKey,payment_provider:payment.code,shipping_provider:shipping.code}});await enqueueIntegrationJob({orderId:order.order_id,kind:'email_send',provider:process.env.EMAIL_PROVIDER||'resend',payload:{template:'order_confirmation'}}).catch(()=>undefined)}let paymentRedirectUrl:string|undefined;if(payment.flow==='online_redirect'){const identity=await getCommunicationIdentity();const callbackUrl=`${identity.siteUrl}/api/payments/${encodeURIComponent(payment.code)}/webhook`;const result=await getPaymentGatewayAdapter(payment.adapterKey).createPayment({orderId:order.order_number,total:{amount:order.total_gross_huf,currency:'HUF'},returnUrl:`${identity.siteUrl}/rendeles-sikeres?token=${encodeURIComponent(confirmation.confirmation_token)}`,cancelUrl:`${identity.siteUrl}/rendeles-sikeres?token=${encodeURIComponent(confirmation.confirmation_token)}&payment=cancelled`,callbackUrl,idempotencyKey:`shoperation-${payment.code}-${order.order_id}`,customerEmail:checkout.email});paymentRedirectUrl=result.redirectUrl;await admin.from('orders').update({external_payment_id:result.providerReference,updated_at:new Date().toISOString()}).eq('id',order.order_id);if(!replayed)await admin.from('order_events').insert({order_id:order.order_id,event_type:'payment_started',metadata:{provider:payment.code,provider_reference:result.providerReference,callback_url:callbackUrl}})}const status=payment.flow==='online_redirect'?'pending_payment':payment.flow==='bank_transfer'?'pending_transfer':'processing';return NextResponse.json({ok:true,replayed,orderId:order.order_id,orderNumber:order.order_number,confirmationToken:confirmation.confirmation_token,subtotal:order.subtotal_gross_huf,discount:order.discount_gross_huf,shippingFee:order.shipping_gross_huf,total:order.total_gross_huf,couponCode:order.coupon_code,status,paymentRedirectUrl},{status:replayed?200:201})}catch(error){return NextResponse.json({error:error instanceof Error?`A fizetés előkészítése nem sikerült: ${error.message}`:'A rendelési szolgáltatás átmenetileg nem elérhető.'},{status:503})}}
