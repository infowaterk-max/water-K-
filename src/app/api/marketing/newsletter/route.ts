import{NextResponse}from'next/server';import{z}from'zod';import{createAdminClient}from'@/lib/supabase/admin';const schema=z.object({email:z.string().trim().email().max(320),consent:z.literal(true),source:z.string().trim().max(80).default('storefront_newsletter')});export async function POST(request:Request){let raw:unknown;try{raw=await request.json()}catch{return NextResponse.json({error:'Érvénytelen kérés.'},{status:400})}const parsed=schema.safeParse(raw);if(!parsed.success)return NextResponse.json({error:'Adj meg érvényes e-mail címet és fogadd el a hozzájárulást.'},{status:400});const admin=createAdminClient(),email=parsed.data.email.toLowerCase();const{error}=await admin.from('marketing_consents').insert({user_id:null,email,channel:'email',status:'granted',source:parsed.data.source,policy_version:'2026-08-v1',metadata:{actor:'visitor'}});if(error)return NextResponse.json({error:'A feliratkozás most nem sikerült.'},{status:500});return NextResponse.json({ok:true,message:'Sikeres feliratkozás.'},{status:201})}
