import { notFound } from 'next/navigation';
import Link from 'next/link';
import { formatHuf, products } from '@/lib/catalog';
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();return <main className="section"><div className="shell heroGrid"><div className="glass"><span className="eyebrow">Water-K {p.size}</span><h1 className="sectionTitle">{p.name}</h1><p className="lead">{p.short}</p><div className="price">{formatHuf(p.grossPrice)}</div><p className="muted">Nettó {formatHuf(p.netPrice)} · készlet: {p.stock} db</p><div className="actions"><Link className="btn btnPrimary" href={`/kosar?add=${p.slug}`}>Kosárba</Link><Link className="btn btnGhost" href="/webaruhaz">Másik méret</Link></div></div><aside className="card"><span className="badge">Használat</span><h2>Gyökérzónába dolgozva</h2><p className="muted">A terméket a növény gyökérzónájához közel kell a talajba dolgozni, majd alaposan beöntözni. Az adagolási kalkulátort a következő fejlesztési csomagban kötjük rá.</p></aside></div></main>}
