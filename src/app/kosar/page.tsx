import { CartView } from '@/components/cart/cart-view';
import { ProductRecommendations } from '@/components/catalog/product-recommendations';
import { getProducts } from '@/lib/catalog-server';
import { getCommerceSettings } from '@/lib/commerce/settings';
import { getRecommendationRules } from '@/lib/recommendations/server';
import { requireStorefrontAccess } from '@/lib/storefront/access';
export const dynamic='force-dynamic';
export default async function Cart(){await requireStorefrontAccess();const[products,rules,commerce]=await Promise.all([getProducts(),getRecommendationRules('cart'),getCommerceSettings()]);return <main className="section cartShowcase"><div className="shell"><div className="commerceMastInner"><div><span className="showcaseKicker">Kosár</span><h1 className="sectionTitle">Már csak néhány lépés.</h1></div><p className="lead">Ellenőrizd a kiválasztott termékeket és mennyiségeket, majd folytasd a biztonságos pénztárhoz.</p></div><div className="commerceSteps"><span className="active">1 · Kosár</span><span>2 · Adatok és szállítás</span><span>3 · Fizetés és rendelés</span></div><div className="cartAssurance"><div><strong>Biztonságos folyamat</strong><span>A rendelési adatok ellenőrzötten kerülnek feldolgozásra.</span></div><div><strong>Valós készlet</strong><span>A rendszer rendeléskor is ellenőrzi az elérhetőséget.</span></div><div><strong>Átlátható végösszeg</strong><span>A díjakat a véglegesítés előtt egyértelműen látod.</span></div></div><CartView freeShippingThreshold={commerce.freeShippingThreshold} products={products.map(product=>({id:product.id,name:product.name,slug:product.slug,grossPrice:product.grossPrice,minimumQuantity:product.minimumQuantity,orderMultiple:product.orderMultiple}))}/><ProductRecommendations products={products} rules={rules} context="cart"/></div></main>}
