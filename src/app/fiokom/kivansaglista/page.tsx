import Link from 'next/link';
import { redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
import { formatHuf } from '@/lib/catalog';
import { getProducts } from '@/lib/catalog-server';
import { getCurrentWebshopInstance } from '@/lib/instances/access';
export default async function WishlistPage(){const supabase=await createClient(),{data:auth}=await supabase.auth.getUser();if(!auth.user)redirect('/fiokom?next=/fiokom/kivansaglista');const instance=await getCurrentWebshopInstance();if(!instance)redirect('/fiokom');const[{data},catalog]=await Promise.all([supabase.from('wishlists').select('id,created_at,variant_id,product_variants!inner(id,label,gross_price_huf,stock_quantity,products!inner(name))').eq('instance_id',instance.id).eq('user_id',auth.user.id).order('created_at',{ascending:false}),getProducts()]);const items=data??[],byId=new Map(catalog.map(product=>[product.id,product]));return <main className="section accountPage"><div className="shell"><div className="sectionIntro"><div><span className="eyebrow">Saját fiók</span><h1 className="sectionTitle">Kívánságlistám</h1><p className="lead">Az elmentett termékek egy helyen, aktuális árral és készletinformációval.</p></div><Link className="btn" href="/fiokom">Vissza a fiókhoz</Link></div><div className="cards">{items.map(item=>{const variant=item.product_variants as unknown as{id:string;label:string;gross_price_huf:number;stock_quantity:number;products:{name:string}},product=byId.get(variant.id);if(!product)return null;return <article className="card" key={item.id}><span className="badge">{variant.stock_quantity>0?'Raktáron':'Nincs készleten'}</span><h2>{product.name}</h2><div className="price">{formatHuf(variant.gross_price_huf)}</div><p className="muted">Készlet: {variant.stock_quantity} db</p><Link className="btn btnPrimary" href={`/termek/${product.slug}`}>Termék megnyitása</Link></article>})}</div>{!items.length&&<section className="card"><h2>Még nincs mentett terméked.</h2><p className="muted">A termékoldalakon a Kívánságlistára gombbal tudsz későbbre elmenteni termékeket.</p><Link className="btn btnPrimary" href="/webaruhaz">Termékek böngészése</Link></section>}</div></main>}
