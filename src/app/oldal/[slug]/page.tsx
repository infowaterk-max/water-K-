import type { Metadata } from 'next';import Link from'next/link';import{notFound}from'next/navigation';import{getPublicPageBySlug}from'@/lib/content/server';import{getCurrentWebshopInstance}from'@/lib/instances/access';import{RichContent}from'@/components/content/rich-content';export const dynamic='force-dynamic';export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,item=await getPublicPageBySlug(slug);if(!item)return{};return{title:item.seoTitle??item.title,description:item.seoDescription??item.excerpt??undefined,alternates:{canonical:`/oldal/${item.slug}`},openGraph:{type:'website',title:item.seoTitle??item.title,description:item.seoDescription??item.excerpt??undefined,url:`/oldal/${item.slug}`}}}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,item=await getPublicPageBySlug(slug);if(!item)notFound();const instance=await getCurrentWebshopInstance(),base=(instance?.brand.publicSiteUrl??process.env.NEXT_PUBLIC_SITE_URL??'http://localhost:3000').replace(/\/$/,''),brandName=instance?.brand.name??'Webáruház',structured={'@context':'https://schema.org','@type':'WebPage',name:item.title,description:item.seoDescription??item.excerpt??undefined,url:`${base}/oldal/${item.slug}`,dateModified:item.updatedAt,isPartOf:{'@type':'WebSite',name:brandName,url:base}};return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(structured)}}/>{item.kind==='landing'?<><section className="hero"><div className="shell"><span className="eyebrow">{item.title}</span><h1>{item.heroTitle??item.title}</h1>{(item.heroSubtitle??item.excerpt)&&<p className="lead">{item.heroSubtitle??item.excerpt}</p>}{item.ctaLabel&&item.ctaHref&&<Link className="btn btnPrimary" href={item.ctaHref}>{item.ctaLabel}</Link>}</div></section><section className="section"><div className="shell"><div className="featurePanel"><RichContent body={item.body}/></div></div></section></>:<section className="section contentPage"><div className="shell"><span className="eyebrow">Információ</span><h1 className="sectionTitle">{item.title}</h1>{item.excerpt&&<p className="lead">{item.excerpt}</p>}<section className="card"><RichContent body={item.body}/></section></div></section>}</main>}
