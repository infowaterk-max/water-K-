import type{Metadata}from'next';import{notFound}from'next/navigation';import{getPublicContentBySlug}from'@/lib/content/server';export const dynamic='force-dynamic';export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,item=await getPublicContentBySlug('blog',slug);if(!item)return{};return{title:item.seoTitle??item.title,description:item.seoDescription??item.excerpt??undefined,alternates:{canonical:`/blog/${item.slug}`}}}export default async function BlogArticle({params}:{params:Promise<{slug:string}>}){const{slug}=await params,item=await getPublicContentBySlug('blog',slug);if(!item)notFound();const paragraphs=item.body.split(/\n\s*\n/).filter(Boolean);return <main className="section"><article className="shell confirmationShell"><span className="eyebrow">Blog</span><h1 className="sectionTitle">{item.title}</h1>{item.excerpt&&<p className="lead">{item.excerpt}</p>}<div className="featurePanel">{paragraphs.map((p,i)=><p key={i}>{p}</p>)}</div></article></main>}
