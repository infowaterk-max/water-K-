import Link from'next/link';import{getPublicContent}from'@/lib/content/server';export const dynamic='force-dynamic';export const metadata={title:'Blog',description:'Hasznos cikkek, útmutatók és hírek.'};export default async function BlogIndex(){const items=await getPublicContent('blog');return <main className="section"><div className="shell"><span className="eyebrow">Tudástár</span><h1 className="sectionTitle">Blog</h1><p className="lead">Hasznos cikkek, útmutatók és hírek egy helyen.</p><div className="cards">{items.map(item=><article className="card" key={item.id}><span className="badge">{item.publishedAt?new Date(item.publishedAt).toLocaleDateString('hu-HU'):'Cikk'}</span><h2>{item.title}</h2><p className="muted">{item.excerpt??item.body.slice(0,180)}</p><Link className="textLink" href={`/blog/${item.slug}`}>Tovább olvasom →</Link></article>)}</div>{items.length===0&&<div className="card"><p className="muted">Még nincs közzétett bejegyzés.</p></div>}</div></main>}
