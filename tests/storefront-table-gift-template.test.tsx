import {renderToStaticMarkup} from 'react-dom/server';import {describe,expect,it} from 'vitest';import {StorefrontRuntimeRenderer} from '@/components/builder/storefront-runtime-renderer';import {createStorefrontMultiProductComposerRendererRegistry} from '@/components/builder/storefront-multi-product-composer';import {createStorefrontMultiProductComposerComponentRegistry} from '@/lib/builder/storefront-multi-product-composer';import {PLANS} from '@/lib/plans/catalog';import {TABLE_GIFT_ENGINE_CONTRACT,TABLE_GIFT_HOME_PAGE,TABLE_GIFT_HOME_SECTION_ORDER,TABLE_GIFT_PRODUCT_PAGE,TABLE_GIFT_TEMPLATE_KEY,TABLE_GIFT_TEMPLATE_PACKAGE,TABLE_GIFT_TEMPLATE_VERSION,TABLE_GIFT_VISUAL_DNA} from '@/lib/builder/templates/table-gift';import {evaluateStorefrontTemplateCapabilityGate,planStorefrontTemplateInstallation} from '@/lib/builder/storefront-template-installation';import {validateStorefrontPageDocument} from '@/lib/builder/storefront-runtime';
const capability={plan:'alap' as const,features:PLANS.alap.features};
describe('Scale-out Wave 16 Table & Gift',()=>{
 it('locks the accepted premium occasion/gifting identity and shared engines',()=>{expect(TABLE_GIFT_TEMPLATE_KEY).toBe('food.table-gift');expect(TABLE_GIFT_TEMPLATE_VERSION).toBe(1);expect(TABLE_GIFT_VISUAL_DNA.character).toBe('premium-gifting-occasion-commerce-gastro-curation-elegant-bundle-building');expect(TABLE_GIFT_VISUAL_DNA.journey).toBe('gift-finder-to-curated-sets-to-build-your-gift');expect(TABLE_GIFT_ENGINE_CONTRACT.requiredForFullExperience).toEqual(['E1','E2','E3','E4','E10','E13']);});
 it('ships 14 Alap-compatible presets through the existing E3→E4 registry chain',()=>{const registry=createStorefrontMultiProductComposerComponentRegistry();const gate=evaluateStorefrontTemplateCapabilityGate({template:TABLE_GIFT_TEMPLATE_PACKAGE,componentRegistry:registry,capability});expect(gate.violations.filter(x=>x.severity==='error')).toEqual([]);expect(gate.ok).toBe(true);expect(TABLE_GIFT_TEMPLATE_PACKAGE.pages).toHaveLength(14);expect(new Set(TABLE_GIFT_TEMPLATE_PACKAGE.pages.map(x=>x.pageType)).size).toBe(14);for(const page of TABLE_GIFT_TEMPLATE_PACKAGE.pages){const result=validateStorefrontPageDocument(page,registry,capability);expect(result.ok,`${page.pageType}: ${JSON.stringify(result.violations)}`).toBe(true);}});
 it('locks the approved gifting concepts into a coherent Home composition',()=>{expect(TABLE_GIFT_HOME_PAGE.metadata?.sectionOrder).toEqual(TABLE_GIFT_HOME_SECTION_ORDER);expect(TABLE_GIFT_HOME_SECTION_ORDER).toEqual(['Gift Hero','Shop by Occasion','Gift Finder','Curated Gift Sets','Build Your Gift','Gift Message','Corporate Gift CTA','Gift Story','Reviews','Footer']);});
 it('renders Gift Finder, real-product Gift Builder, occasion navigation and editorial story from shared registries',()=>{const html=renderToStaticMarkup(<StorefrontRuntimeRenderer page={TABLE_GIFT_HOME_PAGE} viewport="desktop" bindingContext={{brand:{name:'Maison Gift',homeHref:'/',copyright:'© Maison Gift'},navigation:{primary:[],footer:[]},content:{giftHero:{title:'Meaningful Gifts'},curatedGiftSets:{title:'Curated Gift Sets'},giftMessage:{title:'Personal Note'},corporateGift:{title:'Corporate Gifts',copy:'Kapcsolat céges ajándékozáshoz.',href:'/kapcsolat'}},collection:{occasions:[{id:'birthday',label:'Születésnap',href:'/webaruhaz?occasion=birthday'}]},finder:{stepTitle:'Kinek keresel?',question:'Mi az alkalom?',options:[{id:'birthday',label:'Születésnap',href:'#birthday'}],progressLabel:'1 / 3',actionHref:'#gift-results',resultStatus:'idle'},composer:{modeLabel:'4 részes ajándék',progressLabel:'1 / 4',slots:[],availableItems:[{id:'tea',label:'Tea Selection',href:'/termek/tea-selection',price:'4 990 Ft',stockLabel:'Raktáron'}],currentSubtotal:'4 990 Ft',currency:'HUF',actionHref:'#build-gift'},catalog:{curatedGiftSets:[{id:'set',name:'Table Selection',href:'/termek/table-selection',price:'12 990 Ft'}]},gift:{message:{preview:'Boldog születésnapot!'}},story:{featured:{title:'A készítő története',excerpt:'Szerkesztett történet.',image:'/storefront-demo/table-gift/story.svg'}},reviews:{rating:4.9,count:22,summary:'Vásárlói tapasztalatok',href:'#reviews'}}} componentRegistry={createStorefrontMultiProductComposerComponentRegistry()} rendererRegistry={createStorefrontMultiProductComposerRendererRegistry()} capability={capability}/>);expect(html).toContain('data-storefront-guided="finder"');expect(html).toContain('data-storefront-composer="builder"');expect(html).toContain('Tea Selection');expect(html).toContain('Table Selection');expect(html).toContain('A készítő története');});
 it('keeps PDP responsive and pairings as separate real products',()=>{const context={brand:{name:'Maison Gift',homeHref:'/'},navigation:{primary:[],footer:[]},product:{name:'Table Selection',description:'Válogatott ajándék.',gallery:[{src:'https://example.com/gift.jpg',alt:'Gift'}],badges:[]},pricing:{displayPrice:'12 990 Ft',compareAtPrice:''},inventory:{stockLabel:'Raktáron'},variant:{optionLabel:'Méret',optionOptions:[{id:'small',label:'Kisebb',href:'#small',available:true}]},commerce:{purchaseLabel:'Kosárba teszem',purchaseHref:'#purchase'},catalog:{giftPairings:[{id:'card',label:'Üdvözlőkártya',href:'/termek/card',copy:'Külön termék'}]},recommendations:{products:[]}};const desktop=renderToStaticMarkup(<StorefrontRuntimeRenderer page={TABLE_GIFT_PRODUCT_PAGE} viewport="desktop" bindingContext={context} componentRegistry={createStorefrontMultiProductComposerComponentRegistry()} rendererRegistry={createStorefrontMultiProductComposerRendererRegistry()} capability={capability}/>);const mobile=renderToStaticMarkup(<StorefrontRuntimeRenderer page={TABLE_GIFT_PRODUCT_PAGE} viewport="mobile" bindingContext={context} componentRegistry={createStorefrontMultiProductComposerComponentRegistry()} rendererRegistry={createStorefrontMultiProductComposerRendererRegistry()} capability={capability}/>);expect(desktop).toContain('span 7 / span 7');expect(desktop).toContain('span 5 / span 5');expect(mobile).toContain('span 12 / span 12');expect(desktop).toContain('Üdvözlőkártya');});
 it('keeps installation draft-only and avoids invented commercial/safety claims',()=>{const serialized=JSON.stringify(TABLE_GIFT_TEMPLATE_PACKAGE);expect(serialized).not.toMatch(/bundle.?sku|fixed.?bundle.?price|guaranteed delivery|gluten.?free|organic certified|only [0-9]+ left/i);const plan=planStorefrontTemplateInstallation({template:TABLE_GIFT_TEMPLATE_PACKAGE,componentRegistry:createStorefrontMultiProductComposerComponentRegistry(),capability});expect(plan.mode).toBe('install');expect(plan.pages).toHaveLength(14);expect(plan.mutationBoundary).toMatchObject({storefrontPageDrafts:true,products:false,variants:false,customers:false,orders:false,b2b:false});expect(plan.demoLifecycle.install.every(x=>x.namespace==='food-table-gift')).toBe(true);});
 it('keeps corporate CTA presentation-only, gift message non-mutating and checkout provider-neutral',()=>{const home=JSON.stringify(TABLE_GIFT_HOME_PAGE);const checkout=TABLE_GIFT_TEMPLATE_PACKAGE.pages.find(x=>x.pageType==='checkout')!;expect(home).toContain('automatikus B2B ár vagy jogosultság nélkül');expect(home).toContain('A sablon önmagában nem ír rendelési adatot');expect(checkout.metadata?.engineBinding).toBe('E4+E13');expect(JSON.stringify(checkout)).not.toMatch(/K&H|khpos|vpos|payment_secret|merchantId/i);});
});
