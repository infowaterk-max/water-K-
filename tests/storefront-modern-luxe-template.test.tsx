import {renderToStaticMarkup} from 'react-dom/server';import {describe,expect,it} from 'vitest';import {StorefrontRuntimeRenderer} from '@/components/builder/storefront-runtime-renderer';import {createStorefrontVisualLayerRendererRegistry} from '@/components/builder/storefront-visual-layers';import {createStorefrontVisualLayerComponentRegistry} from '@/lib/builder/storefront-visual-layers';import {PLANS} from '@/lib/plans/catalog';import {MODERN_LUXE_ENGINE_CONTRACT,MODERN_LUXE_HOME_PAGE,MODERN_LUXE_HOME_SECTION_ORDER,MODERN_LUXE_PRODUCT_PAGE,MODERN_LUXE_SPACING_CONTRACT,MODERN_LUXE_TEMPLATE_KEY,MODERN_LUXE_TEMPLATE_PACKAGE,MODERN_LUXE_TEMPLATE_VERSION,MODERN_LUXE_VISUAL_DNA} from '@/lib/builder/templates/modern-luxe';import {evaluateStorefrontTemplateCapabilityGate,planStorefrontTemplateInstallation} from '@/lib/builder/storefront-template-installation';import {validateStorefrontPageDocument} from '@/lib/builder/storefront-runtime';
const capability={plan:'alap' as const,features:PLANS.alap.features};
describe('Scale-out Modern Luxe template',()=>{it('locks the accepted Jewelry & Accessories direction',()=>{expect(MODERN_LUXE_TEMPLATE_KEY).toBe('jewelry.modern-luxe');expect(MODERN_LUXE_TEMPLATE_VERSION).toBe(1);expect(MODERN_LUXE_VISUAL_DNA.character).toBe('premium-modern-jewelry-accessories-spacious-editorial-luxe');expect(MODERN_LUXE_VISUAL_DNA.categories).toEqual(['Ékszerek','Órák','Táskák','Napszemüvegek','Kiegészítők']);expect(MODERN_LUXE_VISUAL_DNA.palette).toEqual({background:'ivory',accent:'champagne-gold',text:'black'});expect(MODERN_LUXE_VISUAL_DNA.exclusions).toEqual(expect.arrayContaining(['crowded-home','hero-carousel-next-prev','streetwear','neon']));expect(MODERN_LUXE_ENGINE_CONTRACT.requiredForFullExperience).toEqual(['E1','E2','E13']);});it('ships 14 Alap-compatible Page Schema presets',()=>{const registry=createStorefrontVisualLayerComponentRegistry();const gate=evaluateStorefrontTemplateCapabilityGate({template:MODERN_LUXE_TEMPLATE_PACKAGE,componentRegistry:registry,capability});expect(gate.violations.filter(x=>x.severity==='error')).toEqual([]);expect(gate.ok).toBe(true);expect(MODERN_LUXE_TEMPLATE_PACKAGE.pages).toHaveLength(14);expect(new Set(MODERN_LUXE_TEMPLATE_PACKAGE.pages.map(x=>x.pageType)).size).toBe(14);for(const page of MODERN_LUXE_TEMPLATE_PACKAGE.pages){const result=validateStorefrontPageDocument(page,registry,capability);expect(result.ok,`${page.pageType}: ${JSON.stringify(result.violations)}`).toBe(true);}});it('keeps the final airy Home revision rather than the rejected crowded carousel',()=>{expect(MODERN_LUXE_HOME_PAGE.metadata?.sectionOrder).toEqual(MODERN_LUXE_HOME_SECTION_ORDER);expect(MODERN_LUXE_HOME_SECTION_ORDER).toEqual(['Layered Luxe Hero','Category Edit','Signature Selection','Ajándéknak választva','Brand Story','Footer']);expect(MODERN_LUXE_HOME_PAGE.metadata?.heroNavigation).toBe('none');expect(MODERN_LUXE_SPACING_CONTRACT).toEqual({presets:['narrow','normal','airy','custom'],defaultPreset:'airy',controls:['section-gap','inner-padding','column-gap'],responsive:['desktop','tablet','mobile']});});it('keeps every accepted hero marketing element as an independent editable layer',()=>{const source=JSON.stringify(MODERN_LUXE_HOME_PAGE);for(const id of ['modern-luxe-hero-image-layer','modern-luxe-hero-overlay-layer','modern-luxe-hero-decoration-layer','modern-luxe-hero-badge-layer','modern-luxe-hero-title-layer','modern-luxe-hero-subtitle-layer','modern-luxe-hero-cta-layer'])expect(source).toContain(id);expect(MODERN_LUXE_HOME_PAGE.metadata?.layerContract).toBe('hero-image-title-subtitle-cta-badge-overlay-decoration-separate-editable-layers');});it('renders the accepted large close-up hero with merchant-bound layers and spacious sections',()=>{const html=renderToStaticMarkup(<StorefrontRuntimeRenderer page={MODERN_LUXE_HOME_PAGE} viewport="desktop" bindingContext={{brand:{name:'Atelier No. 8',homeHref:'/',copyright:'© Atelier No. 8'},navigation:{primary:[],footer:[]},content:{modernLuxeHero:{badge:'NEW EDIT',title:'THE QUIET STATEMENT',subtitle:'Jewelry and objects.',image:'https://example.com/jewel.jpg',decoration:'/storefront-demo/modern-luxe/decor.svg',ctaLabel:'Explore',ctaHref:'/webaruhaz'},modernLuxeCategories:{title:'The Edit'},modernLuxeSignature:{title:'Signature Selection'},modernLuxeGift:{eyebrow:'GIFT',title:'Ajándéknak választva',copy:'Selected pieces.',image:'https://example.com/gift.jpg',ctaLabel:'Gift Edit',ctaHref:'/webaruhaz'},modernLuxeStory:{eyebrow:'ATELIER',title:'Made to remain.',copy:'Brand story.',image:'https://example.com/story.jpg',ctaLabel:'Story',ctaHref:'/rolunk'}},collection:{navigation:[{label:'Ékszerek',href:'/webaruhaz?cat=jewelry'}]},catalog:{featured:[{id:'ring',name:'Champagne Ring',href:'/termek/ring',price:'89 900 Ft'}]}}} componentRegistry={createStorefrontVisualLayerComponentRegistry()} rendererRegistry={createStorefrontVisualLayerRendererRegistry()} capability={capability}/>);expect(html).toContain('data-storefront-visual="layered-canvas"');expect(html).toContain('THE QUIET STATEMENT');expect(html).toContain('Champagne Ring');expect(html).toContain('Ajándéknak választva');expect(html).toContain('Atelier No. 8');expect(html).not.toMatch(/next|prev/i);});it('keeps PDP ordered/responsive and uses generic options for jewelry/watch/bag categories',()=>{const context={brand:{name:'Atelier No. 8',homeHref:'/'},navigation:{primary:[],footer:[]},product:{name:'Champagne Ring',description:'Polished detail.',gallery:[{src:'https://example.com/ring.jpg',alt:'Ring'}],badges:[]},pricing:{displayPrice:'89 900 Ft',compareAtPrice:''},inventory:{stockLabel:'Raktáron'},variant:{optionLabel:'Méret',optionOptions:[{id:'52',label:'52',selected:true,available:true,href:'/termek/ring?size=52'},{id:'54',label:'54',selected:false,available:false,href:'javascript:alert(1)'}]},commerce:{purchaseLabel:'Kosárba',purchaseHref:'#purchase'},recommendations:{products:[]}};const desktop=renderToStaticMarkup(<StorefrontRuntimeRenderer page={MODERN_LUXE_PRODUCT_PAGE} viewport="desktop" bindingContext={context} componentRegistry={createStorefrontVisualLayerComponentRegistry()} rendererRegistry={createStorefrontVisualLayerRendererRegistry()} capability={capability}/>);const mobile=renderToStaticMarkup(<StorefrontRuntimeRenderer page={MODERN_LUXE_PRODUCT_PAGE} viewport="mobile" bindingContext={context} componentRegistry={createStorefrontVisualLayerComponentRegistry()} rendererRegistry={createStorefrontVisualLayerRendererRegistry()} capability={capability}/>);expect(desktop).toContain('span 7 / span 7');expect(desktop).toContain('span 5 / span 5');expect(mobile).toContain('span 12 / span 12');expect(desktop).toContain('Champagne Ring');expect(desktop).toContain('aria-disabled="true"');expect(desktop).not.toContain('javascript:');});it('keeps install draft-only, demo namespaced and checkout provider-neutral',()=>{const plan=planStorefrontTemplateInstallation({template:MODERN_LUXE_TEMPLATE_PACKAGE,componentRegistry:createStorefrontVisualLayerComponentRegistry(),capability});expect(plan.mode).toBe('install');expect(plan.pages).toHaveLength(14);expect(plan.mutationBoundary).toMatchObject({storefrontPageDrafts:true,products:false,variants:false,customers:false,orders:false,b2b:false});expect(plan.demoLifecycle.install.every(x=>x.namespace==='jewelry-modern-luxe')).toBe(true);const checkout=MODERN_LUXE_TEMPLATE_PACKAGE.pages.find(x=>x.pageType==='checkout')!;expect(checkout.metadata?.engineBinding).toBe('E13');expect(JSON.stringify(checkout)).not.toMatch(/K&H|khpos|vpos|payment_secret|merchantId/i);});});
