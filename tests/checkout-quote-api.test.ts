import{describe,expect,it}from'vitest';import{readFileSync}from'node:fs';import{join}from'node:path';const read=(p:string)=>readFileSync(join(process.cwd(),p),'utf8');
describe('checkout quote API',()=>{it('resolves active tenant and configured shipping before quoting',()=>{const s=read('src/app/api/checkout/quote/route.ts');expect(s).toContain('getCurrentWebshopInstance');expect(s).toContain("['pilot','active']");expect(s).toContain('commerce.shippingOptions.find');expect(s).toContain('quoteTenantCheckout')});it('never accepts client supplied prices or totals',()=>{const s=read('src/app/api/checkout/quote/route.ts');expect(s).not.toContain('unitPrice:');expect(s).not.toContain('subtotal:');expect(s).not.toContain('total:')})});
