import {renderToStaticMarkup} from 'react-dom/server';import {describe,expect,it} from 'vitest';import {StorefrontRuntimeRenderer} from '@/components/builder/storefront-runtime-renderer';import {createStorefrontStoryRendererRegistry} from '@/components/builder/storefront-story';import {createStorefrontStoryComponentRegistry} from '@/lib/builder/storefront-story';import {PLANS} from '@/lib/plans/catalog';import {GALLERY_EDIT_ENGINE_CONTRACT,GALLERY_EDIT_HOME_PAGE,GALLERY_EDIT_HOME_SECTION_ORDER,GALLERY_EDIT_PRODUCT_PAGE,GALLERY_EDIT_TEMPLATE_KEY,GALLERY_EDIT_TEMPLATE_PACKAGE,GALLERY_EDIT_TEMPLATE_VERSION,GALLERY_EDIT_VISUAL_DNA} from '@/lib/builder/templates/gallery-edit';import {evaluateStorefrontTemplateCapabilityGate,planStorefrontTemplateInstallation} from '@/lib/builder/storefront-template-installation';import {validateStorefrontPageDocument} from '@/lib/builder/storefront-runtime';
const capability={plan:'alap' as const,features:PLANS.alap.features};
describe('Scale-out Gallery Edit template',()=>{it('locks the gallery/interior identity and shared engine boundary',()=>{expect(GALLERY_EDIT_TEMPLATE_KEY).toBe('home.gallery-edit');expect(GALLERY_EDIT_TEMPLATE_VERSION).toBe(1);expect(GALLERY_EDIT_VISUAL_DNA.character).toBe('contemporary-interior-design-gallery-concept-commerce');expect(GALLERY_EDIT_VISUAL_DNA.category).toBe('home-living-design');expect(GALLERY_EDIT_VISUAL_DNA.exclusions).toEqual(expect.arrayContaining(['busy-marketplace','rustic-farmhouse','fake-designer-provenance','fake-material-claims']));expect(GALLERY_EDIT_ENGINE_CONTRACT.requiredForFullExperience).toEqual(['E1','E2','E7','E10','E13']);});it('ships 14 Alap-compatible presets through the shared story/commerce registry',()=>{const registry=createStorefrontStoryComponentRegistry();const gate=evaluateStorefrontTemplateCapabilityGate({template:GALLERY_EDIT_TEMPLATE_PACKAGE,componentRegistry:registry,capability});expect(gate.violations.filter(x=>x.severity==='error')).toEqual([]);expect(gate.ok).toBe(true);expect(GALLERY_EDIT_TEMPLATE_PACKAGE.pages).toHaveLength(14);expect(new Set(GALLERY_EDIT_TEMPLATE_PACKAGE.pages.map(x=>x.pageType)).size).toBe(14);for(const page of GALLERY_EDIT_TEMPLATE_PACKAGE.pages){const result=validateStorefrontPageDocument(page,registry,capability);expect(result.ok,`${page.pageType}: ${JSON.stringify(result.violations)}`).toBe(true);}});it('locks the gallery Home journey',()=>{expect(GALLERY_EDIT_HOME_PAGE.metadata?.sectionOrder).toEqual(GALLERY_EDIT_HOME_SECTION_ORDER);expect(GALLERY_EDIT_HOME_SECTION_ORDER).toEqual(['Gallery Hero','Curated Rooms','New Objects','Designer Story','Material Edit','Gallery Grid','Featured Edit','Reviews','Journal','Footer']);});it('renders design objects, material evidence and editorial story surfaces together',()=>{const html=renderToStaticMarkup(<StorefrontRuntimeRenderer page={GALLERY_EDIT_HOME_PAGE} viewport="desktop" bindingContext={{brand:{name:'Edit No. 4',homeHref:'/',copyright:'© Edit No. 4'},navigation:{primary:[],footer:[]},content:{galleryHero:{title:'Objects in quiet space.'},galleryNewObjects:{title:'New Objects'},galleryObjectGrid:{title:'Gallery Grid'},galleryFeaturedEdit:{title:'Featured Edit'},designerStory:{title:'Studio 08'}},collection:{curatedRooms:[{id:'living',label:'Living',href:'/webaruhaz?room=living'}]},catalog:{newObjects:[{id:'lamp',name:'Arc Lamp',href:'/termek/arc-lamp',price:'89 900 Ft'}],materialEdit:[{specKey:'stone',label:'Kő',displayValue:'termékadat szerint'}],galleryGrid:[{id:'vessel',name:'Stone Vessel',href:'/termek/stone-vessel',price:'29 900 Ft'}],featured:[{id:'chair',name:'Linen Chair',href:'/termek/linen-chair',price:'159 900 Ft'}]},reviews:{rating:4.8,count:45,summary:'Vásárlói tapasztalatok',href:'#reviews'},story:{journal:{items:[{id:'studio',storyType:'journal',title:'Studio Visit',href:'/blog/studio-visit',excerpt:'Objects and space.'}]}}}} componentRegistry={createStorefrontStoryComponentRegistry()} rendererRegistry={createStorefrontStoryRendererRegistry()} capability={capability}/>);expect(html).toContain('Objects in quiet space.');expect(html).toContain('Arc Lamp');expect(html).toContain('Stone Vessel');expect(html).toContain('Linen Chair');expect(html).toContain('Studio 08');expect(html).toContain('data-storefront-story="index"');});it('renders PDP 7/12 + 5/12 with E7 object details and supplied dimensions/materials',()=>{const context={brand:{name:'Edit No. 4',homeHref:'/'},navigation:{primary:[],footer:[]},product:{name:'Linen Chair',description:'Kortárs szék.',gallery:[{src:'https://example.com/chair.jpg',alt:'Linen Chair'}],badges:[],keySpecs:[{specKey:'material',label:'Anyag',displayValue:'Len + tölgy'},{specKey:'width',label:'Szélesség',displayValue:'72 cm'}],specGroups:[{groupKey:'object',label:'Object Details',rows:[{specKey:'height',label:'Magasság',displayValue:'78 cm'},{specKey:'finish',label:'Finish',displayValue:'Matt'}]}]},pricing:{displayPrice:'159 900 Ft',compareAtPrice:''},inventory:{stockLabel:'Raktáron'},variant:{optionLabel:'Kárpit',optionOptions:[{id:'sand',label:'Sand',href:'#sand',available:true}]},commerce:{purchaseLabel:'Kosárba teszem',purchaseHref:'#purchase'},content:{productStory:{title:'Object / Studio Note'}},recommendations:{products:[]}};const desktop=renderToStaticMarkup(<StorefrontRuntimeRenderer page={GALLERY_EDIT_PRODUCT_PAGE} viewport="desktop" bindingContext={context} componentRegistry={createStorefrontStoryComponentRegistry()} rendererRegistry={createStorefrontStoryRendererRegistry()} capability={capability}/>);const mobile=renderToStaticMarkup(<StorefrontRuntimeRenderer page={GALLERY_EDIT_PRODUCT_PAGE} viewport="mobile" bindingContext={context} componentRegistry={createStorefrontStoryComponentRegistry()} rendererRegistry={createStorefrontStoryRendererRegistry()} capability={capability}/>);expect(desktop).toContain('span 7 / span 7');expect(desktop).toContain('span 5 / span 5');expect(mobile).toContain('span 12 / span 12');expect(desktop).toContain('Object Details');expect(desktop).toContain('Len + tölgy');expect(desktop).toContain('72 cm');expect(desktop).toContain('78 cm');expect(desktop).toContain('Object / Studio Note');});it('keeps installation draft-only and avoids unsupported provenance/material claims',()=>{const demo=JSON.stringify(GALLERY_EDIT_TEMPLATE_PACKAGE.demoFixtures??[]);expect(demo).not.toMatch(/original designer|authentic vintage|certified marble|handmade in italy|made by|designed by/i);const plan=planStorefrontTemplateInstallation({template:GALLERY_EDIT_TEMPLATE_PACKAGE,componentRegistry:createStorefrontStoryComponentRegistry(),capability});expect(plan.mode).toBe('install');expect(plan.pages).toHaveLength(14);expect(plan.mutationBoundary).toMatchObject({storefrontPageDrafts:true,products:false,variants:false,customers:false,orders:false,b2b:false});expect(plan.demoLifecycle.install.every(x=>x.namespace==='home-gallery-edit')).toBe(true);});it('keeps E13 checkout provider-neutral',()=>{const checkout=GALLERY_EDIT_TEMPLATE_PACKAGE.pages.find(x=>x.pageType==='checkout')!;expect(checkout.metadata?.engineBinding).toBe('E13');expect(JSON.stringify(checkout)).not.toMatch(/K&H|khpos|vpos|payment_secret|merchantId/i);});});
