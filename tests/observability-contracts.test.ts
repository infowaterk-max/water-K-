import{describe,expect,it}from'vitest';import{readFileSync}from'node:fs';import{join}from'node:path';const root=process.cwd(),read=(p:string)=>readFileSync(join(root,p),'utf8').toLowerCase();describe('V21 observability contracts',()=>{it('redacts common pii and secrets before persistence',()=>{const sql=read('supabase/migrations/161_observability_foundation.sql');for(const key of ['email','phone','password','token','authorization','cookie','address','full_name'])expect(sql).toContain(`'${key}'`)});it('keeps observability writes service-role RPC only',()=>{const sql=read('supabase/migrations/161_observability_foundation.sql');expect(sql).toContain('revoke all on public.observability_events from public,anon,authenticated');expect(sql).toContain('grant execute on function public.record_observability_event')});it('health response propagates a correlation id and disables caching',()=>{const src=read('src/app/api/health/route.ts');expect(src).toContain('x-correlation-id');expect(src).toContain("'cache-control':'no-store'");expect(src).toContain('logevent')})});