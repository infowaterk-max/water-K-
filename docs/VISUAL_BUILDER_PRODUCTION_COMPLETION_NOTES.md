# Live screenshot findings closed by this round

- Active viewport must be visually singular and match the canvas breakpoint.
- Inspector must not show stale template config while the runtime renders a bound merchant value.
- Normal mode must not expose page/component keys as primary UI.
- Common component/field labels must be merchant-facing Hungarian.
- Floating toolbar enabled controls must remain visibly actionable.
