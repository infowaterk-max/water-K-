-- Shoperation fresh-customer seed is intentionally empty.
-- Customer products, branding, domains, pricing and credentials are provisioned explicitly.
select 'Shoperation customer baseline: no customer-specific seed data loaded.' as bootstrap_status;
