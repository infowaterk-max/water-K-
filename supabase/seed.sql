-- Shoperation default seed is intentionally customer-neutral.
-- Fresh customer databases start without products, customer content, domains,
-- addresses, credentials or business-specific pricing.
-- Neutral demo/pilot fixtures must be loaded explicitly outside this default seed.

select 'Shoperation neutral seed: no customer-specific data loaded.' as bootstrap_status;
