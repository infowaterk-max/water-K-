import {buildCodebaseAtlas,impactForAtlasPattern,lookupAtlasTerm,releaseClosureForAtlasPatterns,validateCodebaseAtlas,writeCodebaseAtlasArtifacts} from './lib/shoperation-codebase-atlas-runtime.mjs';
const args=process.argv.slice(2),value=name=>{const i=args.indexOf(name);return i>=0?args[i+1]??'':null;};
const atlas=buildCodebaseAtlas(),validation=validateCodebaseAtlas(atlas);writeCodebaseAtlasArtifacts(atlas);
const queryFile=value('--query-file'),lookup=value('--lookup'),closureFile=value('--closure-file');
if(queryFile)console.log(JSON.stringify(impactForAtlasPattern(atlas,queryFile),null,2));
if(lookup)console.log(JSON.stringify(lookupAtlasTerm(atlas,lookup),null,2));
if(closureFile)console.log(JSON.stringify(releaseClosureForAtlasPatterns(atlas,[closureFile]),null,2));
console.log(`Codebase Atlas 2.0: ${validation.ok?'PASS':'BLOCK'}; files=${atlas.summary.indexedNodes}; routes=${atlas.summary.routeNodes}; imports=${atlas.summary.importEdges}; domains=${atlas.architecture.domainCount}; truths=${atlas.architecture.truthOwnerCount}.`);
for(const issue of validation.issues)console.error(JSON.stringify(issue));
if(!validation.ok&&args.includes('--check'))process.exit(1);
