# Shoperation coding instructions

Before editing implementation code, follow the repository-level `AGENTS.md` Development-Time Known Failure protocol. Generate and read the Development Guard, obtain Plan Before Code PASS, then run Edit-Time Guard and Incremental Replay after coherent edit batches. Never bypass the global Known Failure / Procedural Memory system.
